/** New team (HmF-NewTeam-*) — placeholder until slice F1b builds it. */
export function NewTeamDialog(props: {
  open: boolean;
  initialTemplate?: string;
  onClose: () => void;
}) {
  void props;
  return null;
}

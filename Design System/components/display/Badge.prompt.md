Small pill for status and metadata — muted, warm tones. Reserve `accent` for genuinely brand-worthy labels.

```jsx
<Badge>Draft</Badge>
<Badge variant="accent">New</Badge>
<Badge variant="success" dot>Connected</Badge>
<Badge variant="outline">v0.4</Badge>
```

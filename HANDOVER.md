# HANDOVER — for Tvashtr-56

**You are Tvashtr-56.** Read this + `PROJECTPLAN.md` (root) before doing anything. You are the **architect/planner ONLY** — all implementation goes through Claude Code; you design, write `/goal`s, hand the operator a copyable launch package, then **audit on disk**.

## §0 — Standing operator directives (carry forward EVERY session)
- **Role:** architect/planner ONLY. ALL code (product, scripts, build/Makefile) goes through Claude Code (the CLI `/goal` loop, launched `--dangerously-skip-permissions`). Your only direct edits: `PROJECTPLAN.md`, `HANDOVER.md`, `prompts/*.md`, trivial doc/typo fixes. When tempted to write code, write a `/goal` instead.
- **The disk audit is the real control point** — never trust Claude Code's self-report. Read the files it changed, verify git from `.git/` plumbing (there is NO git CLI in the MCP — use `.git/refs/heads/<b>`, `.git/logs/refs/heads/<b>`), confirm tests are mutation-real (they must FAIL on pre-fix code), diff "untouched" claims against a pre-image.
- **Launch package = 3 copyable blocks inline, EVERY time** (never "same as before", never point the operator at a file for the init/goal text): (1) the shell command, (2) the FULL init prompt verbatim, (3) the FULL `/goal` verbatim. The detailed brief MAY live in `prompts/*.md` (the `/goal` references it), but the init+goal text are always reproduced inline.
- **Init-prompt REQUIREMENTS (Tvashtr-54 — in EVERY init prompt):** explicitly tell Claude Code to (1) after its 5-line summary, **STOP and WAIT** — do NOT begin implementation until the operator pastes the `/goal`; (2) when executing the `/goal`, **USE ultracode, dynamic workflows, and the superpowers skills**.
- **Parallel batches = TWO DIFFERENT features (Tvashtr-54):** always pick two genuinely different tasks/features for the two parallel Claude Code sessions — never two halves of one feature. Disjoint files (overlap map FIRST), each its own worktree + Postgres DB + Vite port, ≤1 migration across the batch.
- **Standing ask:** every init prompt + `/goal` must tell Claude Code to write a DETAILED final report (files changed, exact commands+output, deviations, open questions, next step).
- **`/goal` scope:** one full bounded milestone, verifiable in a single transcript. Acceptance runs ALL checks itself (`make test`, lint, live smoke/e2e) + debugs to green + echoes evidence into chat; never hand the operator commands to run. **Reproduce-first for bug fixes** (a failing test that PROVES the bug, confirmed failing on current code, BEFORE the fix). Stop conditions distinguish "a second/unknown or unproven-broad change → NEEDS_HUMAN" from "a code-proven, contained, regression-guarded fix → proceed".
- **Merges are operator-only, always FF** (`git merge --ff-only`). Give ALL merge commands as copyable text every time (cd → checkout main → merge --ff-only → verify log → optional branch -d), state the expected tip sha + what to do if the FF is refused (stop, don't force). The agent commits only its own changed paths on its branch; never pushes (the no-push + migration-freeze PreToolUse hooks survive bypass). **Merge mechanics reminder (Tvashtr-55 correction):** when two parallel branches fork from the SAME base, only the FIRST fast-forwards; the second diverges and must be **cherry-picked** (`git cherry-pick -n <sha>`, resolve any scratch-file conflict, `git commit -C <sha>`). "FF each, order-independent" is WRONG for same-base pairs.
- **Design decisions:** derive from the vision (§1) + the Tvashtr-25 pivot, NEVER from effort. One design question at a time, decided directly (not an option-menu — reserve menus for genuine strategic-direction calls), and PAIR every decision with its concrete frontend/UX consequence (what the user sees + does on the canvas).
- **Visual/manual sign-off:** default to Claude Code's Playwright self-sign-off (screenshots); when you DO ask the operator to check by eye, give a NUMBERED, click-by-click script (exact team/screen, node by its VISIBLE label, exact action, explicit pass/fail).
- **Operator comms:** terse ("proceed"/"go"/"merged"/"done" = ratify + move; "by the way" = wants a SHORT answer). Procedures ONE STEP AT A TIME (give step 1, wait for the reported output, then step 2). Simple everyday language. Analogies help. Always hand over copyable artifacts.

## Current state (Tvashtr-55 closeout)
- **`main` @ `1b6c0c1`.** History: `1b6c0c1`(C9, cherry-picked) → `968c845`(hygiene, FF) → `61e1f12`(Tvashtr-54 C8) → `226c4b5`(U3) → `733a707`(Tvashtr-53 docs). Everything is MERGED + disk-audited + re-verified green.
- **alembic head `0024`; migration freeze `0001–0024`** (UNCHANGED — BOTH C9 + hygiene were ZERO-migration; freeze hook regex `2[0-4]`). Floors **486 backend / 282 vitest** (486 = 471 base + 16 C9 tests − 1 hygiene-removed dead test). All green (make test / make test-frontend / make lint all clean on merged `main`).
- **The repo is `main`-only + worktree-list clean.** Tvashtr-55 swept 24 stale/merged branches + 2 cherry-picked Tvashtr-53 fillers and pruned 2 orphaned filler worktrees. `git worktree list` shows only the main path. Fresh ground for the next parallel batch.
- **DeepSeek is the go-forward agent model** — `.env`: `TVASHTR_AGENT_MODEL=deepseek/deepseek-chat`, `DEFAULT_MODEL=deepseek/deepseek-chat` (gitignored). Use `deepseek-chat`, NOT `deepseek-reasoner`. NIM (`nvidia_nim/meta/llama-3.3-70b-instruct`) was HARD-DOWN through Tvashtr-53 (connects but never returns, ~181s/attempt); moot now that we're on DeepSeek.
- **The self-imposed `engines/` freeze is RETIRED** — M-robust (Tvashtr-53) made the FIRST sanctioned `engines/` change (`_payload_of`). Future `engines/` work is allowed WITH CARE (keep it surgical + justified). The MIGRATION freeze is SEPARATE + still enforced by the hook.
- **Default sandbox mode is LOCAL** (in-process); docker is opt-in (isolated real-repo work). Both adapters share `_payload_of`, so the M-robust fix covers BOTH (docker confirmed live).
- **STATE.md is now gitignored** (hygiene `git rm --cached`'d it + added it to `.gitignore`) — the CLI agents' running log no longer rides onto `main`. The §15 STATE.md-on-main item is CLOSED. (If a future cherry-pick still bundles a STATE.md commit from a worktree, `git rm STATE.md` during the `-n` stage; it's untracked on `main` now.)

## Nothing in flight — the C9 ‖ hygiene batch is DONE + merged
There is **no open parallel batch** and nothing waiting to be collected. Tvashtr-55 finished Tvashtr-54's two-session batch end to end (collected → audited → merged → cleaned up → docs). **M-rails now has all three content checks** — `secret_leak_scan` (C8) + `diff_touches_forbidden_paths` + `output_schema_check` (C9) — plus the per-owner credential invariant, so the milestone's **content-check surface is COMPLETE** (further gate kinds only on-trigger). Your first real work is the **M-unify U2 design pass** (below).

## What shipped last session (Tvashtr-55; all merged; full as-built in §17, 2026-07-10)
Collected + disk-audited + merged Tvashtr-54's launched two-session batch (both ZERO-migration, file-disjoint). The branch refs corrected the handover: hygiene had ALREADY run + committed; C9 was still at base waiting for its `/goal`.
- **Tech-debt hygiene** (`968c845`, plain FF): deleted the two symbols orphaned when U1 retired the thinker path — `resolve_thinker_max_tokens` (`context_compiler.py`) + the `thinker_max_output_tokens` Setting + its `TVASHTR_THINKER_MAX_OUTPUT_TOKENS` env alias (`config.py`) — de-dangled every reference, kept the 5 live siblings; `git rm --cached STATE.md` + gitignored it. Removed 1 dead test → backend floor 471→470 (goal-sanctioned).
- **M-rails C9 guardrail checks** (`0bfcd0e` → cherry-picked `1b6c0c1`): two NEW deterministic guardrail gate kinds in the openhands-free `guardrails.py` (stdlib+dbos only, mirroring C8) — `diff_touches_forbidden_paths` (config `forbidden_paths` globs; `_git_changed_files` = sorted union of `git diff --name-only HEAD` + `git ls-files --others --exclude-standard`, fail-open; rejects first glob hit naming file+glob) + `output_schema_check` (config `output_file`+`schema`; stdlib JSON-Schema-subset validator; rejects missing/non-JSON/off-schema naming file+first-failing-key-path). `guardrail_gate_step` gained a `config: dict` param; **team_run.py's ONLY change is the gate-arm call (line 1292) passing `cfg`** (human `wait_at_gate` path line 1305 byte-identical). `routers.py` stores the request field `output_schema` under config key **`schema`** (a Pydantic-v2 field literally named `schema` shadows `BaseModel.schema` — sound rename). FE gate-type picker kept as **segmented buttons** (not `<select>`) to preserve the C8 e2e/`getByRole("button")` selectors.
- **Disk audit** (read the code, not the reports): grep-confirmed the dead symbols gone + 5 siblings present (the suite passing at 470 proves nothing dangles); read `guardrails.py` line-by-line — both new fns deterministic + **REDACTING** (reject reasons name files/globs/dotted-key-paths, NEVER secret values or file contents; tests plant a sentinel value + assert its absence); confirmed the one-line `team_run.py` change + the byte-identical human path; the 7 reject tests go RED against always-approve stubs (mutation-real). Merged-`main` re-verified GREEN (486 / 282 / lint).
- **Repo hygiene:** swept 24 stale/merged branches (data-loss-proof `git branch --merged main | … | xargs git branch -d`, `-D` for 2 cherry-picked fillers) + pruned 2 orphaned Tvashtr-53 filler worktrees → repo is now `main`-only + worktree-list clean.

## The M-unify design (the ACTIVE milestone — carry the detail)
- **D1 loop-always:** every node runs the FULL agent loop. Sandbox reuse is **U2** (a hard requirement, scoped to a node's OWN subsequent goals).
- **The pull manifest is the SOLE "edits-not-allowed" enforcer** (tool detachment is steering only); the Slice-4 emitting-node verdict-only pull survives INDEPENDENTLY of the toggle.
- **A compiler-appended capability NOTE, not persona-swap** (only when edits-off; edits-on byte-identical).
- **A file-based deliverable — REPORT.md at the workspace root** + **D2 the ENTRY node owns the spec** (its REPORT.md → the next spec version; a missing REPORT.md fails the invocation cleanly; non-entry reports surfaced not versioned; downstream reads SPEC.md).
- Sliced: **U1 + U3 (both DONE) → U2 (sandbox reuse, NEXT — a deferred HARD req that needs an architect DESIGN PASS first, see the horizon).**

## On the horizon (operator-sequenced)
1. **M-unify U2 — sandbox reuse (NEXT; needs a DESIGN PASS before any brief).** Reuse ONE agent sandbox across a node's subsequent goals. Tracing the seam on disk found it is NOT a fire-and-forget `/goal`: `agent_run_step` creates the sandbox inside a `@DBOS.step`, so a live handle can't cross the step boundary → reuse needs a **process-level adapter cache OUTSIDE the durable model**; the **inviolable `EngineAdapter` seam** (§3) limits the mechanism to additive `AgentTask` fields; **conversation-carry** (OpenHands re-attach carries the prior round's conversation) + the **container reaper** are real semantic calls. Design these **one at a time, each paired with its canvas/UX consequence**, get explicit sign-off, THEN brief. **Latency baselines to beat** (entry-node spin-up→REPORT.md): DeepSeek local 12.2s · docker 20.2s · gpt-4o-mini 8.1s.
2. **The next parallel batch** — after U2 is designed+shipped (or alongside, if two genuinely-different disjoint features present themselves): two DIFFERENT features, overlap map FIRST, ≤1 migration across the batch, each its own worktree + Postgres DB + Vite port. (M-rails' content-check surface is COMPLETE, so further guardrail kinds are on-trigger only, not a default next slice.)
3. **M-memory — agentic memory/RAG** (committed by Sidechat-7, 2026-07-09; Postgres→pgvector, a per-node Memory drawer, brownfield-phase as first consumer) — needs its OWN design pass, then when scheduled.
4. **Brownfield thesis re-run** — deferred, awaiting the right milestone window (now attemptable on DeepSeek + the condenser).
5. **Mv — a non-founder shipping real value on their own repo** — the REAL product gate, off the phase ladder (= the Wizard-of-Oz demand probe, ≥1 non-founder paying user).
- **Company/legal (operator's side):** Tvashtr was explored as a sole proprietorship first; Udyam registration hit a PAN-validation issue (a suspected Aadhaar-PAN name mismatch) — checking Aadhaar-PAN linking at the Income-Tax portal was the next step.

## Gotchas + working notes (not already in PROJECTPLAN)
- **Two chat sequences share this project memory:** "Tvashtr-X" (this — the main build sequence) and "Tvashtr Sidechat-X" (side planning/brainstorming, NOT the build loop). State which you are from the opening message.
- **Git worktrees for parallel sessions** are SIBLINGS of the repo (e.g. `../Tvashtr-x`) — OUTSIDE the Filesystem tool's allowed scope, so you CANNOT read their files directly; audit AFTER the FF-merge (or cherry-pick) lands them in the main checkout. Each worktree needs its OWN Postgres DB + Vite port (5174/5175…).
- **Same-base parallel branches:** only the FIRST FF-merges; the second diverges → **cherry-pick** it (`git cherry-pick -n <sha>` → resolve scratch conflicts → `git commit -C <sha>`). Verify FF-eligibility from the reflog (`.git/logs/refs/heads/<b>`) before promising a clean FF.
- **`prompts/*.md` you author are NOT visible inside a worktree** (untracked, separate working tree) — so for a worktree run the `/goal` is the BINDING brief; always inline the full spec, never "see prompts/…". (In the MAIN checkout, prompts/ IS visible.)
- **File reads:** `copy_file_user_to_claude` caches by basename (a re-copy of a CHANGED file returns the STALE cached version) — use `Filesystem:read_text_file` (cache-free; supports `head`/`tail`) for anything that may have changed; stash a pre-image before re-copying to diff "untouched" claims.
- **Editing the living docs:** use `Filesystem:edit_file` (oldText/newText, EXACT match) for surgical changes, or `Filesystem:write_file` (full overwrite) for a near-total rewrite; the CONTAINER's `str_replace` does NOT reach the user disk (it will say "File not found"). `PROJECTPLAN.md` is ~1090 lines — the header banner is ONE giant line 6 (prepend the new session summary, demote the prior to `_Prior:_`); the §17 append anchor = the last entry's tail + `\n\n---\n\n## 18. Glossary`.
- **litellm `timeout=` does NOT bound a connected-but-hung call** (the NIM failure mode) — a SIGALRM wall-clock guard would; logged in §15.
- **§15 micro-refinements** still open (none blocking): OUTCOME.json rename; edits-on-emitting-node consent UX; per-node doc routing; propose-mode; the `/goal` Stop-hook can't encode a NEEDS_HUMAN terminal; the C9 4-option gate-type picker uses inline `flexWrap` rather than a `.tv-seg` segmented-4 treatment (additive polish). The `resolve_thinker_max_tokens`/`thinker_max_output_tokens` dead-code item + the STATE.md-on-main item are now CLOSED (hygiene shipped both).

## Ready-to-paste opener for the next chat
> You are Tvashtr-56. Read `HANDOVER.md` (especially §0 + Current state) and `PROJECTPLAN.md` at the project root first; don't start work until you've read both. There is **nothing in flight** — Tvashtr-55 collected, audited, merged, and cleaned up the C9 ‖ hygiene batch (`main` @ `1b6c0c1`, head `0024`, floors 486/282, repo main-only). M-rails' content-check surface is COMPLETE. Your first real work is the **M-unify U2 DESIGN PASS**: ratify its decisions — the process-level adapter-cache reuse mechanism (the sandbox is created inside `agent_run_step`, a `@DBOS.step`, so a live handle can't cross the step boundary), the additive-`AgentTask`-field mechanism that respects the inviolable `EngineAdapter` seam, conversation-carry, and the container-reaper interaction — **one at a time, each paired with its concrete canvas/UX consequence**, get sign-off, THEN write the brief. After U2: the next parallel batch (two DIFFERENT features, overlap map FIRST, ≤1 migration, own worktree+DB+port each). Standing rules: launch package = 3 copyable blocks inline every time; every init prompt tells Claude Code to STOP-and-WAIT for the `/goal` + USE ultracode/dynamic-workflows/superpowers; disk audit is the control point; hand over PROACTIVELY as context fills. Mv (a non-founder shipping real value) remains the real gate.

# HANDOVER — Tvashtr-13 → Tvashtr-14

> Written 2026-06-18 by **Tvashtr-13** at the close of **P1.5a** (the cyclic loop: built, visible, crash-durable mid-loop, provably terminating). Read this, then **`PROJECTPLAN.md`** (the source of truth) in full before doing anything. Deep history lives in PROJECTPLAN §15 (roadmap + deferred register) and §17 (decision log); this file is orientation + the **P1.5b** framing. The canonical record of P1.5 is the **five Tvashtr-13 §17 entries** — the P1.5 *design* (Q1–Q4) entry plus the four as-built entries (executor / canvas / crash+cap / the harness-fix line folded into the crash entry). Read the design entry and the three as-built entries.

---

## 0. TL;DR — where we are
- **Phase 1:** P1.1 ✅, P1.2 ✅, P1.3 ✅, P1.4 ✅, **P1.5a ✅ COMPLETE & LIVE-PROVEN**. The hardcoded 2-node `PM → Engineer` line is now a **generic edge-following executor running a real cyclic team** — **PM → Engineer ⇄ Reviewer** — visible on the canvas, durable across a `kill -9` *mid-cycle*, and provably terminating (a `max_review_iterations` cap → a `review_escalation` blocker).
- **`main`** = `5921ac6` (this Tvashtr-13 P1.5a doc closeout) on `5c601ca`+`a41e65a`+`d7d87a6`+`0547370`+`7d2ce08`+`19a5727`. Clean, linear. The merged `feat/p1.5a-*` branches can be pruned. **One untracked file to fold into your first doc commit: `prompts/P1.5a-loop-crash-harness-fix.md`** (the harness-fix brief — Claude Code left it uncommitted per the brief; add it to the archive).
- **Next step: P1.5b — gates as first-class graph entities + the Tasks-for-Human drawer.** That's where you pick up. The **visual smoke-test is at full force** now (the canvas is live) — P1.5b adds gate *nodes* + the blockers/nudges drawer, so design a visible slice and expect an operator eyeball sign-off as part of acceptance.
- P1.5 as a whole proves **M1** (the first real, non-trivial feature from an idea through the loop) — that lands in **P1.5c**, not yet.

## 1. What just landed — P1.5a (Tvashtr-13), in 3 prompts + a fix
The cyclic primitive. Full detail in the three as-built §17 entries; the essentials:
- **The generic edge-following executor** (`backend/tvashtr/control_plane/team_run.py`, merged `19a5727`). `run_team` now walks nodes/edges: a node emits an **outcome label** → the executor follows the edge whose `conditions == {"when": outcome}`, else the unconditional edge, else `None` → exit. A pure `next_node(edges, source, outcome)`; `Edge.conditions` jsonb (migration **`0008`**). The **Reviewer** is a `completion` node (cheap; judges the deliverable + PRD → `approved` | `changes_requested`); the **Engineer ⇄ Reviewer** subgraph cycles. **Termination (D4):** the Reviewer approves OR the cap `config.max_review_iterations` (default **3**) trips at the would-be 4th Engineer iteration → a high-priority `review_escalation` blocker (approve → ship-as-is `completed`; reject → `rejected`). New `AgentInvocation` rows carry **per-iteration state** (`run_id, node_id, iteration, status, outcome`, idempotent on `(run_id,node_id,iteration)`); per-iteration `{run}:agent-cost:{n}` metering. **Scope honesty (important for 5b):** 5a realized the generic walk for the **Engineer⇄Reviewer subgraph only** — the PM, PRD gate, budget gates, ship, and finalize **stay as fixed pre/post phases** wrapping the subgraph. Folding *those* into the uniform walk is exactly P1.5b.
- **The cyclic canvas** (`frontend/src/canvas/*`, `lib/status.ts`, `lib/api.ts`, merged `0547370`). PM → Engineer ⇄ Reviewer rendered with a **calm static dashed 'changes requested' loop-back arc** (`ReworkEdge.tsx`, no animation — DS calm vocabulary), live per-node status, a **'round N' chip** (≥2). `deriveNodeStatus` collapsed to a **thin read of backend truth** (the `status`/`iteration` fields), not run-level archaeology. **Operator visual sign-off** (run `25f98f1f`): the PRD gate paused the Engineer (the `predecessorDone` overlay), approve fired the loop, round-2 chips appeared, it shipped once. **Minor cosmetic nits noted → deferred to P1.5c.**
- **The mid-loop crash proof + the escalation-cap tests** (merged `a41e65a` + harness fix `5c601ca`; **purely additive** — scripts + tests, no product change). `make loop-crash` (`scripts/loop_crash_demo.sh` + `check_loop_crash.py`): runs the loop forced to 2 loop-backs, `kill -9`s the backend **mid-Engineer-iteration-2**, restarts, and asserts DBOS resumes *mid-cycle* and **keeps cycling** to one ship. **LIVE-PROVEN** (run `57cbceb8`): the keystone `engineer_run_attempts pids = [OLD,OLD,NEW,NEW]` — iteration 1 replayed (not re-run), iteration 2 re-ran in the new process, iteration 3 ran post-resume — yet exactly-once-per-iteration durable state. *(The harness's first live run hit a detection race — the trivial task makes iterations ~1s; fixed by killing the instant the loop is mid-cycle then asserting the frozen dead-process state. Harness-only; executor + checker were always correct. The lesson: a poll-then-kill crash harness must kill **before** reading the crash-state.)* The **cap tests** (`backend/tests/test_review_cap.py`, offline, agent stubbed → no openhands) prove D4 enforced termination — Engineer stops at exactly the cap, the escalation blocker fires, approve→ship-as-is / reject→rejected. Offline: **119 tests**, lint clean.

## 2. The immediate next step — P1.5b (gates as first-class graph entities + the Tasks-for-Human drawer)

### What it is / why
Today the gates are **inlined at fixed points** in `run_team` (the PRD gate before the Engineer; the P1.2 budget gates; the new `review_escalation` gate at the cap) — they are *not* graph entities, and `run_team` is still "fixed pre/post phases wrapping a generically-walked subgraph." P1.5b makes **gates first-class entities the executor walks uniformly** and **folds the inlined PM/PRD/budget/ship/finalize phases into the single generic walk**, so `run_team` becomes one uniform node/edge executor with no special-cased phases. It also lands the **Tasks-for-Human drawer** — the §15 item whose trigger is exactly P1.5's reality of concurrent blockers (High/blockers vs Low/nudges, J4) — evolving today's single run-banner Tasks-for-Human card. This is the second visible slice (gate nodes on the canvas + the drawer), so the operator eyeball returns.

### Before designing — audit these (the session-opening pattern)
- **`backend/tvashtr/control_plane/team_run.py`** — the now-partly-generic `run_team`: the `run_review_loop` workflow-body helper (the uniform subgraph walk), `next_node`, `load_graph_step`, and the **fixed pre/post phases** still wrapping it (PM phase → PRD gate → pre-engineer budget → `engineer_setup` once → the loop → outcome routing → pre-ship budget → ship → finalize). P1.5b folds those phases into the walk. Study how each inlined gate (`wait_at_gate`) pauses + resolves.
- **`backend/tvashtr/control_plane/gates.py`** — the gate mechanism (`wait_at_gate`, `gate_auto_resolution_step`, the HumanTask + `DBOS.recv`/resolve-endpoint signalling). P1.5b turns these into graph-placed gate entities.
- **`backend/tvashtr/control_plane/teams.py`** — `build_review_loop_team` (the hardcoded 3-node builder). P1.5b likely adds **gate nodes** to the authored graph (the builder stays hardcoded; the Supervisor swaps it in P1.8).
- **`backend/tvashtr/models.py`** — `TeamGraph` / `AgentNode` (now with `kind`: agent/completion) / `Edge` (`conditions` jsonb). P1.5b probably needs a **gate node `kind`** (prd_approval / budget / review_escalation) the executor pauses at, and the canvas renders — **plausible migration `0009`** (flag it early).
- **`backend/tvashtr/routers.py`** — `GET /api/runs/{id}/graph` (the canvas reads it; may need to express gate nodes) + the `/tasks` + `/tasks/{id}/resolve` endpoints (the drawer reads/acts on these).
- **The frontend** — `frontend/src/canvas/*` (render a **gate node** type), `panel/SidePanel.tsx`, and the **Tasks-for-Human card → drawer** (High/blockers vs Low/nudges). `lib/status.ts` already reads backend truth. Note: the FE has **no linter/RTL tests** (§15) — gate is `tsc` + `vitest`-unit only.

### Design questions to open with the operator (just-in-time, ONE at a time, deciding — not menu-ing)
1. **Gate representation.** A first-class **gate node** in the graph (its own `kind`, a position, that the executor pauses at and follows an approve/reject edge from)? Or a gate *attribute* on an edge? Pick the model that makes the executor's walk uniform AND the canvas render gates naturally.
2. **Folding the fixed phases into the uniform walk.** What's the final node taxonomy (agent / completion / gate / terminal)? How do the PM, ship, and finalize phases become nodes so `run_team` has no special-cased pre/post?
3. **The Tasks-for-Human drawer.** The High/blockers vs Low/nudges inbox (J4) — how it surfaces *concurrent* gates (e.g. a PRD gate + a budget gate both pending), and the canvas visual for a paused gate node. The DS calm bar + the n8n visual quality bar are part of acceptance.
4. **Scope of 5b vs 5c.** Per-node budgets (§15 agent-slice item, trigger P1.5) — likely stays per-run for 5b. The reviewer-verdict view + canvas nits + the real feature are 5c. Confirm 5b's slice stays the gates+drawer.

## 3. Working method (UNCHANGED — the prompt-based loop)
Architect writes **one self-contained Claude Code prompt** → **save it as a `.md` in `/Users/adimac/Desktop/Tvashtr/prompts/`** (naming `P{phase}.{step}{letter}-{slug}.md`; never bury it inline with separator lines) so it runs via `/tvashtr-loop <path>` or a paste → operator runs it → pastes the report → **architect audits the disk against the report** (never trust the report over the disk — reports have arrived garbled; read the actual files) → operator smoke-tests (**visual check at the center for the visible slices**) → operator merges (branch-per-step, operator-merges, usually FF). **ALL implementation goes through the prompt** — product code, throwaway/diagnostic scripts, build/Makefile/config alike (a recurring drift point: when tempted to write code directly, write a prompt; the only architect-direct edits are the two living docs, prompt/brief files in `prompts/`, and trivial doc/comment/typo fixes). Prompts must be **near-exhaustive**: exact paths + current snippets, a precise do-NOT-touch list, granular ordered tasks with anchors, a **Task 0 verify-don't-assume** (capture vendor/SDK specifics on the REAL run path), the specific tests to write/run, and a report-back spec (files changed, commands run *with output* incl. `make test` + lint, deviations, open questions, next step). **Operator standing directive: the architect makes the vision-grounded call DIRECTLY — surface design *questions* one at a time, but decide rather than offer a menu.** The `/tvashtr-loop` skill stays for mechanical steps; **Playwright MCP** is sanctioned for automated UI/E2E *inside* a prompt (complementing, never replacing, the operator's manual visual check). **Communication (userPreferences):** procedures **one step at a time** (operator completes a step + reports before the next — used heavily); terse approvals = proceed; "By the way" = short answer; analogies help.

## 4. Carried / open items (near-term; full register in §15)
- **Deferred to P1.5c (don't lose):** the **reviewer per-round verdict view** (tiny backend `AgentInvocation.outcome` in the graph endpoint + a panel showing the reviewer's per-round verdicts) — explicitly moved out of P1.5a; the **minor canvas cosmetic nits** the operator noted at the visual sign-off (details TBD with the operator); plus 5c's core — the **real non-trivial feature + canonical-idea persistence + cyclic-canvas polish to the n8n bar**.
- **Docker-mode loop seeding** (§15, trigger = before P1.5c's real **multi-file** feature on the **default-docker** path). 5a/the loop proofs run **local** (orchestration, not containment); in docker mode the agent-server container is empty per run, so an iteration-2 container can't see iteration-1's work — the symmetric fix is to **push** the host workspace into the container at iterations >1 (the loop's P1.3 analog). Land it ahead of 5c.
- **Agent-native resume** (§15; Decision-1 restart) — on crash the workspace is NOT reset, so a re-run can ship a *different* deliverable (exactly-once is count, not content). **Trigger is P1.5c's long real-code loop — may bite there.**
- **FE lint/format + RTL component tests** (§15, new this session) — the frontend has no linter/formatter or component tests (gate is `tsc`+`vitest`-unit only); pairs with 5c's canvas polish or a dedicated FE-infra step.
- **Reviewer-as-agent upgrade** (§15) — the review node `completion`→`agent` when the deliverable grows to a multi-file repo the reviewer must *explore*; likely in/after 5c.
- **Proxy budget-429 retry-backoff** (§15) — a multi-minute finalize delay after a budget cutoff (no extra spend); trigger = P1.5's longer loops. **Metering-collapse** (route the PM through the proxy + authoritative proxy spend) — staged.
- **WebSocket transport** (P1.6, after 5c's first ship — the loop's event bursts strain ~1.8s polling). Plus: agent-server image **digest pinning**; `scripts/` outside the ruff gate; per-run container targeting (image-based reaping is the correct serial fallback); proxy loopback-binding hardening; doc-layer/TipTap (P1.7); model catalog/picker UI; CRDT (Yjs) doc concurrency.

## 5. Gotchas / environment
- **Project root `/Users/adimac/Desktop/Tvashtr`** (capital T — casing matters). **Filesystem MCP ONLY** for project file reads/writes (confirm `list_allowed_directories` at session start). **Never** use the built-in bash/`str_replace`/`view`/`create_file` on project files — those hit Claude's own container, not the operator's machine. *(Deferred `Filesystem:*` tools can get evicted mid-session; if one returns "tool not found", re-run `tool_search` naming it, e.g. "Filesystem write_file".)*
- **The loop env vars (how to drive the demos):** the Makefile does `include .env; export`, so `make backend` loads `.env` into the recipe env. To force loop behavior reliably use **make command-line variables** (highest precedence, auto-exported): `make backend TVASHTR_AGENT_SANDBOX=local TVASHTR_FORCE_REVISIONS=1` (omit `TVASHTR_AUTO_APPROVE_GATES` so the PRD gate pauses for a visual check). The three knobs: **`TVASHTR_AGENT_SANDBOX`** (`local`/`docker`), **`TVASHTR_AUTO_APPROVE_GATES`** (`1` = auto-approve every gate — all-or-nothing), **`TVASHTR_FORCE_REVISIONS=N`** (the Reviewer forces `changes_requested` while its iteration `n<=N`, then `approved` — a deterministic, no-cost forced loop so proofs are non-vacuous).
- **The loop make targets:** `make loop-run` (live 3-node loop, local + auto-approve + FORCE=1 → Engineer ×2, one loop-back, ships once); `make loop-crash` (the live mid-loop crash proof, FORCE=2 → Engineer ×3, `kill -9` mid-iteration-2; ~2–3 min, real spend, needs port 8000 free). `make skeleton-run`/`skeleton-crash` (the 2-node line, still green). The **cap tests are offline** (in `make test`). To run the app visually: `make backend …` in one terminal + `make frontend` (Vite → `localhost:5173`) in another.
- **A stale DBOS workflow `b57aa838`** keeps **re-recovering on each backend boot** (a greeting run stuck at its PRD gate with no UI to approve it). Harmless — proofs scope to their own run_id — but it clutters logs and re-recovers when `make loop-crash` boots its uvicorn. If a tidy slate is wanted, cancel it via the run-cancel mechanism (the P1.1 kill switch) — verify the exact endpoint first.
- **The proxy is opt-in and OFF for the loop demos.** Keep `LITELLM_PROXY_ENABLED=0` (or unset) in `.env`; keep `LITELLM_MASTER_KEY`. The loop demos pin `local` and don't use the proxy. Only `proxy-budget-demo` self-enables the proxy (+docker). (If the proxy is left on, a per-run budgeted key is minted, which would change `budget-demo`/`loop-run` behavior — see the P1.4 gotcha.)
- **Editing the living docs:** `edit_file` with **`dryRun: true` first** for PROJECTPLAN (long anchors silently no-op on a whitespace mismatch — anchor on unique multi-line strings; the real-apply diff IS reliable); `write_file` for a full HANDOVER rewrite. The §17 log is append-only (insert new entries before the `---` that precedes `## 18. Glossary`). **The header "Last updated" blockquote had drifted (stuck at Tvashtr-10) — now current at Tvashtr-13; keep it bumped.**
- **Stack:** DBOS Transact (durable workflows), LiteLLM (gateway + proxy + virtual keys), React Flow (canvas), OpenHands via the Software Agent SDK (default engine), TipTap (doc editor, P1.7), OpenRouter (default provider). Env: macOS Apple Silicon, Claude Code, uv, Node 24, Docker. Pins: litellm **1.89.0**, openhands-sdk **1.28.1**, dbos **2.23.0**, Python 3.12.
- **Tests/lint:** `make test` = **119** offline tests (needs `make db-up && make migrate` first — Postgres + the proxy via docker compose, though offline tests don't touch the proxy). `make lint`/`fmt` = ruff over `backend/` only (the **frontend has no linter** — `tsc --noEmit` + `vitest` only; §15). The import-boundary purity file is `tests/test_registry.py`; `import tvashtr.main` stays openhands-free at startup. Migrations: head is **`0008`** (down_revision `0007_run_budget_caps`); P1.5b may add `0009` (a gate node kind) — flag it.
- **Docker is the default sandbox** (`main.py` lifespan boot-sweep at startup, no-op when clean). The loop/skeleton fast demos pin `local`; the docker path is exercised by `*-docker` + `containment-*`. The git convention: feature work on a branch, **the operator merges** (FF); the architect commits the living-doc closeout.

## 6. First step for Tvashtr-14
Read this + PROJECTPLAN in full (especially the five Tvashtr-13 §17 entries: P1.5 design Q1–Q4, and the executor / canvas / crash+cap as-builts) → confirm the Filesystem path + a clean git state (**fold the untracked `prompts/P1.5a-loop-crash-harness-fix.md` into a tidy commit; the merged `feat/p1.5a-*` branches can be pruned**) → audit the P1.5b surfaces (§2 above: `run_team`'s remaining fixed phases + the `run_review_loop` walk, `gates.py`, `teams.py`'s builder, the `AgentNode`/`Edge` schema, the graph + tasks endpoints, and the **frontend** — the gate-node render + the Tasks-for-Human card→drawer) → open **P1.5b** design with the operator **one question at a time, deciding rather than offering menus** (recommended first: **gate representation** — a first-class gate node the executor pauses at vs a gated edge, the model that makes the walk uniform AND renders on the canvas). Then write the first sub-step's Claude Code prompt, saved to `prompts/`. **Design the first slice to be visible — the operator's eyeball is part of acceptance again.**

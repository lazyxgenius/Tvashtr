# HANDOVER → Tvashtr-23

> **State at handoff:** `main` @ `cf02770` (unmoved) · alembic head `0011` · **P1.7a built, disk-audited zero-blocking, FF-READY but NOT yet merged** on branch `feat/p1.7a-steering-backend` (`37fba6c`, 2 ahead of main, clean FF). Working tree also carries **2 uncommitted `PROJECTPLAN.md` §15 edits** from Tvashtr-22 (see §7).
> Read this, then `PROJECTPLAN.md` §1 (the vision — load-bearing this session) + §13–§17. The two newest §15 register entries (the §14.3 smoke deferral; the agentic-memory named-upgrade) are Tvashtr-22's. The §17 log's newest entry is still Tvashtr-21's — the **P1.7a as-built §17 entry is a DRAFT in §4 below, to be applied post-merge.**

---

## 0. Who you are
You are **Tvashtr-23**, the **architect/planner** chat. You do **not** write product code. Loop: scope a lean Claude Code `/goal` → operator launches it (bypass) → **you audit on disk** (read the actual changed files, diff against baselines, verify git refs — never trust the report) → operator FF-merges → you update the living docs. Your only direct edits: `PROJECTPLAN.md`, `HANDOVER.md`, `prompts/*.md`.

---

## 1. FIRST ACTION — finalize P1.7a (operator was mid-merge)
Tvashtr-22 shipped + audited **P1.7a (live-document steering — the backend seam)** but left it FF-ready, NOT merged, pending the live gate. Confirm with the operator:
1. **Did `make loop-crash` / `loop-run` / `skeleton-crash` / `skeleton-run` pass** (the live proof the live PRD re-source composes with crash-resume)? These are operator-run (need a live LLM/NIM). If not yet, ask them to run `loop-crash` first.
2. **Did they FF-merge** `feat/p1.7a-steering-backend` (`git checkout main && git merge --ff-only feat/p1.7a-steering-backend` → main becomes `37fba6c`)? Eyeball the FF `--stat` = ~10 files (the 3 product files `service.py`/`routers.py`/`team_run.py` + 5 test files + the brief + STATE.md) and **nothing** under `alembic/versions/`, `frontend/`, `engines/`, `docker_runtime.py`, `shipping.py`, `teams.py`.

**Once both confirmed:** apply the §4 doc drafts (the §17 as-built entry + the header bump), annotate the §15 P1.7 roadmap bullet, and **commit `PROJECTPLAN.md` (which also carries Tvashtr-22's 2 uncommitted §15 edits) + `HANDOVER.md` together** (`docs(tvashtr-22): closeout — P1.7a as-built §17, §15 smoke+memory, P1.7 progress; HANDOVER for Tvashtr-23`). Then start **P1.7b** (§6).

### What P1.7a is (audited as-built)
The in-flight PRD is now a **live steering surface** (J3 — "the change propagates to the relevant agents on their next read; the document, not agent memory, is the source of truth"). A new recorded **`@DBOS.step read_latest_prd_step(run_id)`** reads `Run.pm_document_id` → the latest `DocumentVersion` content (raises loud if absent), called at **every agent-node entry** (Engineer + Reviewer, every iteration) and passed to both agents — **replacing the PM's once-captured `prd_text` snapshot** (removed). The **idea stays the immutable snapshot** (only the PRD is live). It's a *recorded* step for crash-resume determinism (its content is checkpointed + replayed verbatim → a resumed walk reads the same version the original saw even after a post-crash edit). New **`POST /api/documents/{id}/versions`** (over the existing idempotent `add_version`; fresh key per save; 404/400 mirroring the GET) + **`get_latest_version`** service helper. **No migration, no frontend, no executor-contract/adapter/`teams.py`/`pm_step` change.** 177 offline green (+6, incl. a non-vacuous keystone steering test proven by mutation-revert + a structural is-a-step guard). Architect disk-audit (executor/endpoint/service diffed vs the main baseline; git FF-chain; the @DBOS.step decorator; keystone non-vacuity; no 0012) = **zero blocking**.

---

## 2. The P1.7 design (locked with the operator this session — the spine of what's next)
**The fork was settled: build the §13 value-proven path (option B), and P1.7 (live-doc steering) goes FIRST, then P1.8 (Supervisor-first onboarding).** Rationale (operator-checked against the §1 vision, not effort): the painkiller is the live versioned source-of-truth document + review gate; P1.7 *builds the painkiller*, P1.8 *smooths entry into* the vitamin (team authoring) — so the painkiller comes first; P1.7 is also lower in the dependency stack (every run flows through the document layer) and deepens the one n=1-validated value signal.

**The steering mechanic — LOCKED as re-read-before-write at node entry, via a recorded `@DBOS.step`.** This is the literal J3 mechanism ("propagates on their next read") + the Risk-2/Q4 optimistic-concurrency resolution ("agents re-read before write"), **not** a work-minimizing shortcut. The two alternatives were rejected for *deviating from J3*, not for being hard: an explicit "apply my edit now" signal is a **J2** concept (team-graph reshape "applies mid-session or deferred per preference") — J3 makes document steering *passive*; re-plan-on-edit is a bigger capability J3 doesn't call for. Both stay later rungs if a real need surfaces.

**The slice decomposition — standard backend-then-FE, no throwaway:** **P1.7a = the backend steering seam (DONE).** **P1.7b = TipTap editor + lock-aware UI + the live J3 visual smoke.** (Tvashtr-22 briefly floated "minimal-editor-first" then scrapped it — it would build a throwaway editor TipTap immediately replaces, the same anti-pattern the budget-nudge deferral avoided.) The split is driven by **risk-seam isolation** (the re-read's DBOS-determinism seam deserved its own focused audit + the FE/TipTap seam is separate), the established project discipline.

**The document soft-lock is a DELIBERATE deferral (registered).** In the current loop only the PM *writes* the PRD; the Engineer/Reviewer only *read* it, so a human edit never collides with an agent write and a document-level advisory lock would be inert. Add it when an agent re-writes a doc the human also edits (the §13 Risk-2 ladder: doc-lock → section-locks → CRDT).

---

## 3. Strategic + memory context (from this session)
- **Agentic memory — a §15 named-upgrade was logged** (operator asked for research). The key call: **Tvashtr is already positioned correctly by the durable-execution design** — working + episodic memory already lives in Postgres (DBOS state + `RunEvent` + `AgentInvocation` + versioned `Document`/`DocumentVersion`), which IS the best-practice "keep the raw event stream as your own source of truth." **No memory framework (Mem0/Letta/Zep) now**; the named first upgrade is **`pgvector` on the existing Postgres** when cross-session/long-horizon recall becomes real — almost certainly **brownfield (Phase 3)**. **The operator's distinction (carry it into P1.7b):** the user-facing per-agent `.md` is a **presentation** artifact (Document layer), NOT agent memory — keep `Document`s as user-facing/steering artifacts, do NOT overload them as agent memory.
- **§14.3 visual smoke = SKIPPED for velocity** (operator call), logged §15 as a tracked deferral (aesthetic-only; correctness disk-audited + offline-green). Run it before any external demo of the A/B view, or when canvas-polish is taken up.
- **Freeze-extension still pending for P1.8:** `protect-migrations.sh` freezes `0001`–`0009`; `0010`/`0011` are merged but outside the frozen range. **Before P1.8's migration-bearing `/goal`, extend the freeze to `0001`–`0011`** (via a `/goal`/brief — it's a hook script, implementation goes through Claude Code). P1.7b is FE-only (no migration), so this can wait.

---

## 4. Doc-update DRAFTS — apply post-merge (see §1)

### §17 as-built entry (DRAFT — append after the Tvashtr-21 entry, before `## 18. Glossary`; confirm the merge sha)
> **2026-06-24 (Tvashtr-22) — P1.7a (live-document steering — the backend seam) BUILT, disk-audited zero-blocking, and FF-merged to `main` (`37fba6c`, FF from `cf02770`).** The first P1.7 slice: the in-flight PRD becomes a live steering surface (J3 — the document, not agent memory, the source of truth). Backend-only (feat `5c19828`). As-built (architect diffed the executor/endpoint/service vs the main baseline, not the report): a new recorded `@DBOS.step read_latest_prd_step(run_id)` reads `Run.pm_document_id` → the latest `DocumentVersion` content (raises loud on a missing doc/version), called at EVERY agent-node entry (Engineer + Reviewer, every iteration) and passed to both agents, REPLACING the PM's once-captured `prd_text` snapshot (removed); the idea stays the immutable snapshot. Recorded-step for crash-resume determinism (checkpointed + replayed verbatim → the resume reads the same PRD even after a post-crash edit; a structural test guards it stays a step). New `POST /api/documents/{id}/versions` (over the idempotent `add_version`; fresh key per save → every POST a new version; 404/400 mirroring the GET) + `get_latest_version` (latest by `version_no` desc). `pm_step` byte-identical; NO migration (head `0011`), NO frontend, NO executor-contract/adapter/`teams.py` change. **177 offline green** (+6, incl. the non-vacuous keystone steering test — drives the real `run_team`, appends a PRD version between Engineer round 1 and the forced revision, asserts round-2 receives the edit; proven by mutation-revert: `get_latest_version` desc→asc makes the round-2 assertion fail); adversarial 2-agent review 0 blocking. One necessary consequence: the existing offline `run_team` test fakes (random-uuid `pm_step`) had to persist a real v1 doc so the live re-read resolves → factored into a shared `seed_pm_prd` conftest helper (no assertion relaxed). Architect disk-audit zero-blocking (git FF-chain clean, 2 ahead rooted on `cf02770`; the executor diff is exactly the change; `read_latest_prd_step` IS a `@DBOS.step`; keystone non-vacuous; no `0012`). **Live composition gate GREEN** (Tvashtr-22 post-P1.7a): `make loop-run` (2f080e81) + `make loop-crash` (5912a671 — crash-resume held: pids span pre→post-crash, resumed mid-iteration-2, shipped once) each ship exactly once. The original Engineer-agent failures were the **qwen3-next-80b OpenHands serialization flake** (`TextContent not JSON serializable`) — NOT a P1.7a bug, not auth/quota — fixed config-only by switching `.env TVASHTR_AGENT_MODEL` → `nvidia_nim/meta/llama-3.3-70b-instruct` (same NIM key; product code byte-identical to `5c19828`, architect-verified). The design (fork → B/P1.7-first; re-read-before-write mechanic grounded in J3 + Risk-2/Q4; backend-then-FE no-throwaway split; the document soft-lock deferred) is in §2 of the Tvashtr-23 HANDOVER. **TipTap editor + lock-aware UI + live J3 visual smoke = P1.7b.**

### Header "Last updated" (DRAFT — replace the current line)
> **Last updated:** 2026-06-24 — **Tvashtr-22: P1.7a (live-document steering — backend seam) BUILT + disk-audited zero-blocking + FF-merged (`main` @ `37fba6c`).** A recorded `@DBOS.step read_latest_prd_step` re-sources the latest PRD `DocumentVersion` at every agent-node entry (replacing the PM's `prd_text` snapshot; idea stays the snapshot) + a `POST /api/documents/{id}/versions` save endpoint → a human PRD edit reaches the Engineer/Reviewer on their next read (J3). Recorded-step for crash-resume determinism. NO migration (head `0011`), NO frontend; 177 offline green (+6, non-vacuous keystone). **The fork is settled — building the value-proven path (B): P1.7 first, P1.8 next.** Steering mechanic locked = re-read-before-write (J3); slices = backend (P1.7a ✅) then FE (P1.7b = TipTap + lock-aware UI + live smoke). [then keep the prior "Prior — Tvashtr-21…" chain]

### §15 roadmap annotation (DRAFT)
On the Phase-1 `P1.7` bullet, append: *(P1.7a backend steering seam ✅ Tvashtr-22 — recorded live PRD re-source + save-version endpoint; **P1.7b** = TipTap editor + lock-aware UI + live J3 visual smoke.)*

---

## 5. Execution model + guardrails
The CLI `/goal` loop (bypass `--dangerously-skip-permissions`), governed by `prompts/CLI-RULES.md` + `CLI-SETUP.md`. **Init-prompt HALT fix (don't word it "until"):** the agent outputs its ≤5-line summary then STOPS and ENDS ITS TURN, so the operator pastes the `/goal` as a separate message. For a bigger milestone, point a lean `/goal` at a `prompts/<name>.md` brief (P1.7a did this → `prompts/p1.7a-steering-backend.md`). Guardrails survive bypass (PreToolUse hooks): `protect-no-push.sh` (no push/force/reset), `protect-migrations.sh` (`0001`–`0009` frozen — extend to `0011` before P1.8). Operator FF-merges only; the FF `--stat` is your authoritative file-set gate.

## 6. Next milestone — P1.7b (the FE half of P1.7)
Make the steering surface human-usable + prove J3 live. Scope (design it one question at a time, decide directly): **(a)** the PRD editor — `PrdView` read → **TipTap** (the Q4-resolved Phase-1 editor; ProseMirror, a new FE dep, markdown round-trip) → a save calls the new `POST /api/documents/{id}/versions`; **(b)** lock-aware UI affordance (even if the backend lock is deferred — surface "editing"/version state so the human knows the agents will pick up the save on their next read); **(c)** the **live J3 visual smoke** (the operator eyeball — the real acceptance gate): start a run, edit the PRD mid-run, watch the next agent round build from the edit. Stand-up note: vitest already exists (P1.1b); FE still has **no eslint/prettier** (a §15 item — consider standing it up with P1.7b). Ground the design on disk: `frontend/src/panel/PrdView.tsx`, `App.tsx`, `lib/api.ts`, `documents/service.py`, the new endpoint + `read_latest_prd_step`.

## 7. Gotchas
- **Uncommitted `PROJECTPLAN.md` §15 edits** (the §14.3 smoke deferral + the agentic-memory named-upgrade) are in the working tree, architect-owned — they carry across the FF-merge; commit them WITH the §4 drafts post-merge (don't lose them).
- **The freeze-extension to `0001`–`0011`** is pending — needed before P1.8's migration, not P1.7b.
- **Live composition gate is operator-run** (needs NIM); the offline suite cannot exercise a real kill-9 resume — the "verify on the actual execution path" principle (cf. the docker-mode classification bug) applies, so don't treat offline-green as proof the live re-source survives crash-resume until `loop-crash` is green.
- **Filesystem MCP audit patterns:** `list_allowed_directories` at session start (root `/Users/adimac/Desktop/Tvashtr`, capital T); diff the branch file against a preserved main-state baseline (`copy_file_user_to_claude` → it lands in `/mnt/user-data/uploads/`; cp the baseline aside FIRST since the next copy overwrites); git state from `.git/refs/heads/<branch>` (nested for `feat/x`) + `.git/logs/HEAD` (reflog tail); `edit_file` always `dryRun: true` first, anchor on a unique multi-line string + re-include any trailing structural marker (`---`/`## N.`) in `newText`. MCP can hang (~4-min) — retry/restart.

---

### Ready-to-paste opener for Tvashtr-23
You are Tvashtr-23. Read HANDOVER.md, then PROJECTPLAN.md §1 + §13–§17, at the project root first (Filesystem MCP; `list_allowed_directories` first). Confirm state on disk: `main` @ `cf02770` (or `37fba6c` if the operator already FF-merged P1.7a), branch `feat/p1.7a-steering-backend` @ `37fba6c`, alembic head `0011`. First, finalize P1.7a per HANDOVER §1 — confirm with me whether I ran the live gate (`make loop-crash`/`loop-run`/`skeleton-*`) and FF-merged; if so, apply the §4 doc drafts (the §17 as-built + header bump) and commit PROJECTPLAN.md (with the 2 carried-over §15 edits) + HANDOVER.md. Then open P1.7b (TipTap editor + lock-aware UI + live J3 smoke), one design question at a time. Don't start work until you've read both docs.

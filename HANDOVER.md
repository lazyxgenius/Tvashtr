# HANDOVER — Tvashtr-48 → Tvashtr-49

> Read this FIRST, then the named `PROJECTPLAN.md` sections, then the relevant source on disk — evidence before design. You are the **architect/planner**; all implementation goes through Claude Code.

---

## 0. First message for the next chat
> You are Tvashtr-49. Read `HANDOVER.md` and `PROJECTPLAN.md` at the project root first (HANDOVER first). The FIRST context-management batch is DONE — **M-ctx1 (context compiler) + M-ctx0 (condenser + model bench) shipped and merged** (`main` @ `4b2a221`, alembic head `0019`, floors 373 backend / 198 vitest). Your milestone is **M-ledger** (deep-dive C5 + C6): fix the `RunEvent.seq` cross-invocation collision (additive `invocation_id` + a unique index, migration `0020`, freeze-bumped LAST), assemble the one-row-per-node-execution trajectory ledger view (invocation ⨝ the new `context_manifest` ⨝ cost ⨝ outcome label), add a capture-only `GET /api/runs/{id}/trajectory`, and light up the F1c run-inspector's context-manifest slot — capture-only, NO training / NO judge-LLM / NO GPUs. Design it one decision at a time (pair each with its user-facing UX consequence), write the `/goal`, hand the operator the complete copyable launch package, and audit on disk before merge. Don't start work until you've read both.

---

## 1. Where we are RIGHT NOW
- **`main` @ `4b2a221`.** Alembic head **`0019`** (M-ctx1 added it — the FIRST migration since the freeze; freeze now **`0001–0019`**). Floors on merged `main`: **373 backend** / **198 vitest** (the 373 = 340 baseline + 21 from M-ctx1 + 12 from M-ctx0; always re-baseline from a fresh `make test` / `make test-frontend` at the start of a slice).
- **The FIRST context-management batch SHIPPED.** The operator chose the deep-dive **context chain** (M-ctx0→M-rails) as the next track after M-frontend, *entire*, run as TWO PARALLEL Claude Code sessions. **M-ctx1** (context compiler + input budget + `context_manifest` + doc-handle + raised thinker max_tokens; migration `0019`) and **M-ctx0** (the in-worker summarizing condenser ON in both adapters + a `scripts/model_bench.py` + `make model-bench`) are both merged + disk-audited. See §2.
- **What Tvashtr-48 shipped:** the **next-track decision** (presented the 3 options — the context chain / the brownfield re-run / Mv — with rationale + UX; operator chose the chain, entire) + the **first parallel batch** of that chain. Both milestones DISK-AUDITED, FF/cherry-picked, branches + worktree cleaned up.
- **Working tree:** `PROJECTPLAN.md` + `HANDOVER.md` + the two new `prompts/*.md` (`M-ctx1-context-compiler.md`, `M-ctx0-condenser-bench.md`) are modified/untracked (this closeout) — **they need one commit** (command in §6). The deep-dive `.md` files + `design/` stay untracked, as always. `STATE.md` is git-tracked and was deliberately NOT modified by either agent (they recorded READY_TO_MERGE in-transcript to keep the diff to their own files) — leave it.

## 2. What Tvashtr-48 did — the next-track decision, then a TWO-SESSION parallel batch
**The decision (§3-worthy strategic call):** with the frontend wall lifted, the architect PRESENTED the three tracks (context chain / brownfield stronger-worker re-run / Mv demand probe), each with its one-line rationale + user-facing UX consequence, and let the operator choose (a genuine strategic-direction call). The operator picked **the context chain, "Entirely,"** to be executed via two parallel sessions.

**The batch (M-ctx1 ‖ M-ctx0):**
- **The split was forced by ONE rule:** only ONE session per parallel batch may add a migration (two branches each adding `0019` = two Alembic heads git won't flag). So the pair = **M-ctx1** (the migration-adder) ‖ **M-ctx0** (config-only, the sole no-migration milestone in the chain). Before any `/goal`, the architect READ `team_run.py` and found the decisive seam: the whole worker context string is assembled IN the executor (`instruction = node_prompt + context`, ~L740) and both adapters just forward `task.instruction` → context-compiler work (executor) and condenser work (adapters) are file-disjoint. Handed the operator the file-overlap map FIRST + got approval.
- **Two scoping guards** kept them disjoint: M-ctx1 owns `config.py`; M-ctx0 keeps condenser params inline. M-ctx0 owns the one new Makefile target; M-ctx1 reuses existing smokes.
- **Runtime contention solved by a second DB:** two backend sessions both run `make test` against Postgres → gave **Session B its own `tvashtr_ctx0` database** via a `DATABASE_URL` export in its launch. Verified airtight first: `make migrate`/`make test`/DBOS all resolve the URL from `get_settings().database_url` (`db.py:sqlalchemy_url()`), so the export fully isolates B.
- **Session A = M-ctx1** (main checkout, `be52906`, **FF-merged** first). NEW `control_plane/context_compiler.py` — a PURE `compile_context(...)` (typed parts + `len//4` token measure; the caller writes SPEC.md). C2: per-node INPUT budget (`worker_context_token_budget`~110k + per-node `config.model_config` override) fails PRE-CALL naming the fattest part, reusing the existing terminal-failure shape (NO new edge/outcome label); nullable `agent_invocations.context_manifest` JSONB (mig `0019`). C3: both thinker `max_tokens=400` → `thinker_max_output_tokens`~2048 + override. C4: spec >~1500 tok → SPEC.md handle + pointer + static-first reorder; else inline ORIGINAL order (byte-identical, golden-pinned). floor 340→361.
- **Session B = M-ctx0** (own worktree + own DB, cherry-picked LAST `e26e159`+`4b2a221`; NO migration; executor byte-intact). C1: `LLMSummarizingCondenser(keep_first=2, max_size=80, distinct usage_id via model_copy)` into both adapters' byte-identical `Agent(...)` block. C9: `scripts/model_bench.py` + `make model-bench` (llama-70b/Nemotron-super/Nemotron-nano/Kimi through the gateway, no litellm leak; the REVIEWER seat scored on correctly REJECTING a wrong build — the D4 measure). floor 340→352.
- **Disk audit:** `--name-only` diffs proved ZERO file overlap (17 vs 8, no shared file; `graph_validity.py`+`teams.py` byte-intact by omission). Read A's compiler (pure) + byte-cross-checked its small-path join vs the `team_run.py` pre-image; read the migration (additive nullable) + the freeze hook (bumped to `0001–0019`); read the budget-breach + byte-equivalence tests (mutation-real — the breach test drives a REAL `run_team` and proves the adapter is NEVER reached). B's condenser proven by a real-object-inspection parametrized test; bench no-litellm-leak grep-confirmed. **Combined `make test` on merged `main` = 373 passed.** Cleanup: `git worktree remove --force`; `-d feat/m-ctx1-context-compiler` (merged); `-D feat/m-ctx0-condenser-bench` (cherry-pick rewrote its shas). Full as-built in **PROJECTPLAN §17 (2026-07-04, Tvashtr-48)**.

## 3. IMMEDIATE NEXT STEP — M-ledger (deep-dive C5 + C6; migration `0020`)
The next milestone in the chain. **Details (PROJECTPLAN §15 C5/C6 + §16):**
- **C5 — fix the `RunEvent.seq` cross-invocation collision:** each `run()` restarts `seq` at 0, so per-iteration event rows collide and are silently dropped (a long-standing §15 observability bug — the long loops that drop events are exactly the trajectories the ledger needs). Fix = an additive `invocation_id` on `run_events` + a unique index (migration `0020`). Then assemble the **one-row-per-node-execution ledger view**: `AgentInvocation ⨝ the new context_manifest (mig 0019) ⨝ CostRecord ⨝ the outcome label`. Add a **capture-only** `GET /api/runs/{id}/trajectory`. **Capture-only — NO training, NO judge-LLM, NO GPUs** (that's X13/X9, explicitly deferred).
- **C6 — light up the F1c run-inspector's context-manifest slot:** the run-inspector shell exists from F1c; surface the recorded `context_manifest` (parts + tokens + budget + handle_used) per selected node. This is the FE half — likely a small addition to the run-view drawer.
- **Design it ONE decision at a time**, pair each with its user-facing UX consequence (e.g. "the run inspector now shows, per node, exactly how big each context part was and whether the spec was offloaded to SPEC.md — the user can SEE why a node failed-over-context"). Get sign-off before the next decision. This is NOT a strategic-direction call — DECIDE from the vision, don't menu it.
- Migration `0020` is the SECOND since the freeze — create it NEW, bump the freeze to `0020` as the LAST step of the `/goal`.
- **Could this be a parallel batch?** M-ledger is one coherent backend+FE milestone; it doesn't obviously split into two disjoint sessions (C5 backend + C6 FE could parallelize, but C6 reads C5's ledger shape → coupled). Probably a single session. If the operator wants parallelism, find a genuinely independent second slice (e.g. an optional §15 cleanup) and give the overlap map first.

**`/goal` LENGTH CAP: 4000 characters** (a 5446-char F2c `/goal` was rejected; this batch's were 2823 + 2764). Keep every `/goal` under 4000; lean on a `prompts/*.md` brief for detail (the `/goal` says "Follow prompts/<x>.md exactly"). Verify with `wc -c` before handing it over.

## 4. Standing operator directives (CARRY FORWARD — inherited every session)
- **Role = architect/planner ONLY.** All implementation (product code, diagnostic/throwaway scripts, build/Makefile/config) goes through a Claude Code `/goal`. Architect-direct edits are ONLY: the two living docs, `prompts/*.md` briefs, and trivial doc/typo fixes. When tempted to write code, write a `/goal`.
- **The disk audit is the main control point.** Never trust the self-report — read the changed files, byte-diff "untouched"/"additive-only" claims against a pre-image stashed to `/tmp/`, read test bodies to confirm they're mutation-real, and for a bug-fix confirm the reproduce-first regression genuinely goes RED on the old code. **Scrutinize EVERY deviation from the brief** — read the actual change and judge it (this session: M-ctx0's `max_size=80`/model_copy-usage_id + the 2 extra adapter-test files were all read and judged SOUND; M-ctx1's "measure budget over full content before the C4 offload" was a smart deviation). The **`git diff --stat` / `git diff --name-only main <branch>`** is the free scope proof (→ byte-intact set by omission).
- **No `git` CLI in the MCP** — reconstruct git state from `.git` plumbing: `.git/HEAD` (main checkout's current branch), `.git/refs/heads/<branch>` (tip; `feat/x/y` → `.git/refs/heads/feat/x/y`), `.git/logs/refs/heads/<branch>` (reflog → parent chain + FF-ability + nothing-pushed). A worktree's branch ref lives in the MAIN repo's shared `.git/refs/heads/`. **Operator is your git-CLI proxy** for a diff you can't compute — give ONE clean command (`git diff --stat main <branch> | cat` avoids the pager; a long output opens `less` → tell them to press `q`).
- **The copy-cache gotcha:** `copy_file_user_to_claude` caches by basename — a 2nd copy returns the STALE version. `/mnt/user-data/uploads` is READ-ONLY to bash (can't `mv`/rm the cached copy). To diff a CHANGED file: either use `Filesystem:read_multiple_files` (cache-free, current content → context) or the operator-proxy `git diff` (surgical, low-context). NEW files (never copied) copy fresh, so read them directly.
- **Editing the operator's disk:** `Filesystem:edit_file` (line-based; the returned diff is the verifier — use exact multi-line anchors from a `sed` of the cached copy; `dryRun:true` first for anything delicate) or `Filesystem:write_file` (full overwrite — HANDOVER). `create_file`/`str_replace`/`bash_tool` write to CLAUDE's container ONLY (invisible to the operator). **For a worktree Session-B brief:** the worktree is OUTSIDE the allowed dirs, so write the brief to the MAIN repo's `prompts/` and have the `/goal` point CC at `../Tvashtr/prompts/<brief>.md` (the sibling path — proven this session).
- **Merges: operator FF-merges** (`git merge --ff-only`). ALWAYS give ALL merge commands as copyable text (cd, checkout, merge, verify line + expected sha, what-if-it-fails, branch cleanup). A parallel/worktree branch that DIVERGED merges by **cherry-pick** (`git cherry-pick <sha>` — the shas get REWRITTEN; then `git branch -D` FORCE + `git worktree remove --force <path>`). One step at a time (A-merge, wait for "merged", then B-cherry-pick).
- **Every Claude Code handoff = the COMPLETE copyable launch package, every time** (even if unchanged): (1) `cd /Users/adimac/Desktop/Tvashtr && claude --dangerously-skip-permissions`, (2) the FULL init prompt verbatim, (3) the FULL `/goal` verbatim (**under 4000 chars**). NEVER "same as before"; NEVER point them at a `prompts/` file for the init/`/goal` text. The detailed BRIEF lives in `prompts/<n>.md` (the `/goal` references it). For a worktree Session-B, ALSO give the `git worktree add -b <branch> <path> main` + a `createdb` for its own DB first, and an init prompt telling CC it's already on that branch in a worktree with `DATABASE_URL` exported (do NOT `git checkout`).
- **`/goal` = one bounded milestone**: outcome + invariants-as-on-disk-evidence + the acceptance checklist CC runs ITSELF to green (make test/lint, live smoke, self-sign-off) + reproduce-first for bug-fixes + stop conditions (NEEDS_HUMAN + turn cap; distinguish "unknown/broad → stop" from "code-proven contained fix → proceed"). CC runs ALL verification — never hand the operator manual commands.
- **Design decisions: from the vision (§1) + the Tvashtr-25 pivot, never from effort. One at a time, explicit sign-off before the next. No option menus for vision calls — decide. Pair EVERY decision with its concrete user-facing UX consequence.** (Reserve "present the options and let me choose" for genuine strategic-direction calls — e.g. the Tvashtr-48 next-track decision.)
- **Deferred items → §15 register IMMEDIATELY** (with the decision that deferred them).
- **Visual sign-off = a concrete, NUMBERED, click-by-click script** (name the exact team by rail label + the exact node by VISIBLE label + the exact action + explicit pass/fail). Default to CC's Playwright self-sign-off with a screenshot per check; the operator eyeball is the backstop.
- **Bypass mode caveat** (`--dangerously-skip-permissions`): allow/deny rules go INERT — only PreToolUse hooks survive. `protect-migrations.sh` (freeze, now `0001–0019`) + `protect-no-push.sh` (no-push) are BOTH hooks, so they hold. The agent NEVER pushes; commits only its OWN changed paths (leaves the living docs + `prompts/*.md` for the architect).
- **Communication (operator preferences):** terse — "proceed"/"go"/"merged"/"done" = ratify + move on; "By the way" wants a short answer. **Procedures ONE step at a time** (the operator completes+reports each step before the next — honored strictly this session for the worktree/DB setup + the merges). **Analogies help. Simple, everyday language — avoid jargon.** Always hand copyable artifacts; never make them scroll back or reconstruct.
- **Session hygiene:** watch context; wrap up cleanly BEFORE running out (warn proactively). "handover" is an immediate trigger. Close out = PROJECTPLAN (§17 as-built + §15 register + §16 status + header bump) then a full HANDOVER rewrite then the ready-to-paste opener + the docs-commit command.

## 5. Parallel-session playbook (proven 3×: Tvashtr-46 landing, Tvashtr-47 papercuts, Tvashtr-48 the context batch)
If the operator wants two Claude Code sessions:
- **Give the file-overlap map FIRST and wait for approval** — name the exact files each session touches + where they collide, before any `/goal`. Prove disjointness with `--name-only` diffs AFTER the runs, before merging.
- **The three hard invariants (a merge can't fix them):** (1) NO two migrations in one batch — two branches each adding one = two Alembic heads + a broken chain git won't flag; so the migration-adder pairs with a NO-migration slice. (2) executor (`team_run.py`/`graph_validity.py`) byte-intact in the session NOT deliberately opening it. (3) minimize file overlap (use scoping guards — assign each shared file, e.g. `config.py`/`Makefile`, to ONE session).
- **Two BACKEND sessions CAN parallelize** (Tvashtr-48's new lesson) if the second gets its OWN resource: a second Postgres DB (`createdb tvashtr_<x>` + a `DATABASE_URL` export in its launch) — analogous to the F4-landing's own vite port (5174). Verified the export isolates everything because migrate/test/DBOS all read `get_settings().database_url`.
- **Session B runs in its OWN `git worktree` off `main`** (`git worktree add -b <branch> <path> main`). Its brief lives in the MAIN repo's `prompts/` (the worktree is unreadable to the architect); the `/goal` points CC at `../Tvashtr/prompts/<brief>.md`. Session B's CONTENT audit happens AFTER it lands in `main` (post-cherry-pick, the files are readable).
- **Session A merges FIRST (FF); Session B (diverged off the same base) merges LAST via cherry-pick** (rewrites its shas → `git branch -D` force + `git worktree remove --force`). Then a combined `make test` on merged `main` is the final gate.

## 6. Gotchas & tools
- **Commit the docs closeout** (the only dirty state to commit — includes this session's two briefs):
  ```
  cd /Users/adimac/Desktop/Tvashtr
  git add PROJECTPLAN.md HANDOVER.md prompts/M-ctx1-context-compiler.md prompts/M-ctx0-condenser-bench.md
  git commit -m "docs(tvashtr-48): M-ctx1 + M-ctx0 shipped (first context-management batch, 2 parallel sessions); handover to Tvashtr-49"
  ```
  (The deep-dive `.md` files + `design/` stay untracked. Leave `STATE.md`.)
- **PROJECTPLAN.md is ~270KB / ~900 lines.** NEVER read it whole. Copy it (`copy_file_user_to_claude`) → `grep -nE '^#{1,3} '` for a header index → `sed -n 'A,Bp'` targeted reads. Key sections: §1 vision, §6.4 the human-authored-graph constitution, §14 features, **§15 roadmap + deferred register**, §16 milestones, **§17 append-only as-built log**, §18 glossary.
- **The context-compiler surface (M-ctx1, new):** `backend/tvashtr/control_plane/context_compiler.py` — `compile_context(...)` (PURE) returns a `CompiledContext` with `parts`/`instruction`/`total_tokens`/`budget`/`over_budget`/`fattest`/`handle_used`/`spec_doc` + a `.manifest()` (the JSONB written per worker invocation). The executor (`team_run.py`) calls it, acts on `over_budget` (pre-call terminal failure), writes/removes `<workspace>/SPEC.md`, threads `max_tokens` from `resolve_thinker_max_tokens`. `agent_invocations.context_manifest` (mig `0019`, nullable) — **this is the column the M-ledger view JOINs.**
- **The condenser surface (M-ctx0, new):** both adapters (`openhands_adapter.py` ~L317, `openhands_docker_adapter.py` ~L296) build `Agent(condenser=LLMSummarizingCondenser(keep_first=2, max_size=80, llm=<distinct-usage_id copy>))`. `scripts/model_bench.py` + `make model-bench` — the bench (routes through `tvashtr.gateway`, never imports litellm). Its LIVE run (+ live condensation events) is DEFERRED (needs an NVIDIA key from `main`).
- **Backend key facts:** `Run.id == Run.workflow_id == the DBOS workflow id`. Run-scoped tables (`cost_records` by `workflow_id`; `run_events`/`agent_invocations`/`human_tasks`/`engineer_run_attempts` by `run_id`) have NO FK cascade. **`run_events` `seq` restarts at 0 per `run()` → collisions → M-ledger's C5 fixes this with an additive `invocation_id`.** The executor is `control_plane/team_run.py` + `graph_validity.py` (grep to prove byte-intact on a slice). The SHARED cancel core is `cancel_run_core` in `teams.py`.
- **The seeded dev team is "My team"** = the `review_loop` template (visible labels: Product manager entry, Engineer, Reviewer workers, PRD approval + Escalation gates, Ship + Stop terminals). Bring-up: `make db-up && make migrate && make seed && make backend` + `make frontend` (Vite :5173); login `operator@tvashtr.local` / `tvashtr-dev`.
- **FE status is centralized in `frontend/src/lib/status.ts`.** The design targets live in `design/Tvashtr Frontend Overhaul/*.dc.html` (translate to real React + `.tv-*` CSS; target LOOK not a rebuild). The token set is complete — reskins add `.tv-*` classes to `index.css`, never new tokens.
- **Tools:** Filesystem MCP (operator disk — `read_multiple_files`/`edit_file`/`write_file`/`list_directory`/`copy_file_user_to_claude`; load via `tool_search` at session start), `bash_tool` (Claude's container ONLY — for `/mnt/user-data/uploads/` + `/tmp/` pre-image diffs), Playwright MCP (automated UI/E2E inside a `/goal`). Briefs: `prompts/CLI-RULES.md` + `prompts/CLI-SETUP.md` (operating contracts — the init prompt overrides them with current state).

## 7. Open questions / to-confirm
- **M-ledger is Tvashtr-49's first build task** (§3) — a vision-derived design call (DECIDE, don't menu). Migration `0020`, freeze-bumped last.
- **§15 deferrals standing (non-blocking, none scheduled):** **the LIVE validation of C1+C9** (condensation events in `run_events` on a long run + the full `make model-bench` table — needs an NVIDIA key, run from `main` when quota allows; the M-ctx0 worktree had no `.env`). The brownfield stronger-worker thesis re-run (still one non-throttled `brownfield-rung2` away; M-ctx0's condenser now makes the un-scoped variant attemptable). App-wide modal a11y (focus-trap + Escape). The residual non-MiniMap canvas test-warnings. The F3 onboarding-answers "what they do" + the auth-step BYOK fineprint. Durable run→library-team FK (needs a migration). Orphaned PM `documents` on run teardown. Per-run history drill-down. Plus the older register (C7/C8 remain in the chain; X1–X13 rejects; per-node fallback model; typed-output/multimodal; hosted-OSS serving).
- **The frozen backend contract:** head is now `0019`, executor byte-intact. M-ledger's C5 opens a NEW migration `0020` (freeze bump LAST); M-tools would be `0021`.
- **Mv — the real value gate** (a non-founder shipping real value on their own repo) remains standing, off the phase ladder. The build-first stance holds; the product is now feature-complete enough that "is it time to probe?" is a live strategic question the operator may raise at any point.

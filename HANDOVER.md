# HANDOVER — Tvashtr-47 → Tvashtr-48

> Read this FIRST, then the named `PROJECTPLAN.md` sections, then the relevant source on disk — evidence before design. You are the **architect/planner**; all implementation goes through Claude Code.

---

## 0. First message for the next chat
> You are Tvashtr-48. Read `HANDOVER.md` and `PROJECTPLAN.md` at the project root first (HANDOVER first). **M-frontend is now COMPLETE** — the premium reskin spans tokens → canvas → dashboard → auth → landing, so the frontend wall that gated the roadmap has lifted. Your first job is the **next-track decision**: surface the options for what's next (the deferred M-ctx0…M-rails deep-dive chain, the brownfield stronger-worker thesis re-run, or the Mv demand probe), each with its one-line rationale + UX consequence, and let the operator choose — this is a genuine strategic-direction call, so present the options rather than deciding unilaterally. Don't start work until you've read both.

---

## 1. Where we are RIGHT NOW
- **`main` @ `c486a7f`.** Alembic head **`0018`** (freeze `0001–0018` holds; NO migration all of Tvashtr-47). Floors on merged `main`: **336 backend** / **198 vitest**. (The vitest floor moved 191 → 198 in F3 — LoginScreen's 5 tests deleted with the file, +10 new AuthWizard tests; then the FE-papercuts cherry-pick added +2 → the working number is 200 on `main`, but treat **198** as the F3-slice floor and always re-baseline from a fresh `make test-frontend` at the start of any FE slice.)
- **M-FRONTEND IS COMPLETE.** F0 → F1a/b/c → canvas-fidelity 1–2 → F4 landing (+hardening) → F2 dashboard (a/b/c/delete) → **F3 auth wizard** are all shipped + merged. The premium reskin now spans **tokens → canvas → dashboard → auth → landing** end-to-end. **The deferred wall LIFTS** (see §3).
- **What Tvashtr-47 shipped:** **F3** — the standalone `LoginScreen` is replaced by a single two-panel guided **`AuthWizard`** (account-first; sign-up detours through two throwaway role/building questions; sign-in skips them); plus two **FE-papercuts** cleanups (a MiniMap test-stub + a `NewTeamDialog` `mountedRef` guard). Both disk-audited + merged. See §2.
- **Working tree:** `PROJECTPLAN.md` + `HANDOVER.md` + the two new `prompts/*.md` (`F3-auth-wizard.md`, `FE-papercuts.md`) are modified/untracked (this closeout) — **they need one commit** (command in §6). `STATE.md` carries the spent F3 `READY_TO_MERGE` signal (uncommitted, agent-owned — leave it; the next CC run overwrites it). The deep-dive `.md` files + `design/` stay untracked, as always. Both feature branches + the papercuts worktree were cleaned up (§2).

## 2. What Tvashtr-47 did — ran TWO PARALLEL Claude Code sessions
The operator asked "anything we can do in parallel?"; the architect gave a **file-overlap map FIRST** (F3's files vs the papercuts session's files → **zero shared files**) and the operator approved before any `/goal`.
- **Session A — F3 the auth wizard** (`a136ca7`, off `35f32a6`, **FF-merged**). Designed one decision at a time (operator-ratified): **D1** the wizard REPLACES `LoginScreen` (not a screen after it) — one unified two-panel flow with the account form as a STEP, per `design/Tvashtr Frontend Overhaul/AuthWizard.dc.html`; operator amended the order to **account-FIRST**. **D2** two sign-up questions kept — role (Engineer/Founder/Solo builder) + building (A fresh idea / Against my repo); operator DROPPED the repo-path field + the "we'll inspect your repo" over-promise. **D3** the answers **EVAPORATE** — held in wizard state for the recap chips, DISCARDED on entry, nothing persisted/threaded (§15 holds the deferred "what they do"). As-built: NEW `AuthWizard.tsx` (a `Step` state machine) + `AuthWizard.test.tsx` (10 mutation-real tests); `AuthMode` moved here from `LoginScreen`; account step reuses `api.ts` `login`/`register`; `onAuthed` fires only at "Enter Tvashtr" (identity held in state); `LoginScreen.tsx`+`.test.tsx` DELETED; `AuthGate.tsx` swap (import + one JSX line; `handleAuthed` unchanged, passes only the user; 401 seam intact); `LandingPage.tsx` re-pointed the `AuthMode` import (line 4 only); `.tv-authwiz__*` added to `index.css` (+416/−0). vitest 193→198. FE-only, no migration.
- **Session B — FE-papercuts** (two §15 cleanups; own git worktree off `35f32a6`; **cherry-picked LAST** as `8e1d3de`+`c486a7f`, diverged → new shas, disjoint files → clean). (1) `test/setup.ts` stubs ONLY `MiniMap`→null via an `importOriginal` partial-mock (MiniMap NaN/act test-noise 13→0). (2) `NewTeamDialog.tsx` a `mountedRef` (mirroring `Dashboard`) guarding `create()`'s post-await catch set-states (+2 tests). Fully DATA-LESS (no DB) so zero contention with F3's live Playwright run.
- **Both DISK-AUDITED** (in `main` post-merge): the EVAPORATE wall proven by reading `AuthWizard.enter()` (passes only the user) + `AuthGate.handleAuthed` (unchanged); `api.ts` no F3 contamination + auth exports proven by tsc-strict; the 4 token files BYTE-IDENTICAL vs pre-image (no new tokens); `index.css` a single add-only hunk (`649a650,1065`, 0 removed); `LandingPage.tsx` only the import line; git plumbing confirmed F3 FF-clean + papercuts diverged→cherry-pick, both UNPUSHED; every new test read + mutation-real; the 2 papercuts fixes read (MiniMap-only mock; the guard wraps only the catch). Full as-built in **PROJECTPLAN §17 (2026-07-04, Tvashtr-47)**. Cleanup done: `git worktree remove --force …Tvashtr-fe-papercuts`; `git branch -d feat/f3-auth-wizard`; `git branch -D feat/fe-papercuts` (force — the cherry-pick rewrote its shas).

## 3. IMMEDIATE NEXT STEP — the next-track decision (M-frontend is DONE)
With M-frontend complete, the wall that deferred the roadmap has lifted. This is a **genuine strategic-direction call** (the roadmap is explicitly "operator-sequenced") — so **present the options and let the operator choose**, don't pick unilaterally. The main candidates (all in PROJECTPLAN §16 + §15):
- **M-ctx0 — Condenser + model bench** *(config-only, NO migration; the lowest-friction start)*: turn the OpenHands `LLMSummarizingCondenser` ON in the worker adapters (C1) + a scripted 3-model bench (C9: llama-3.3-70b vs Nemotron-3 Super/Nano vs kimi-k2.6). **Designed to PAIR with the brownfield thesis re-run** — it attacks the two known rung-2 walls (context-overflow + the 70b capability/reviewer ceiling). If the operator wants to resolve the long-standing stronger-worker verdict, this is the pairing.
- **The brownfield stronger-worker thesis re-run itself** — one NON-THROTTLED `make brownfield-rung2` (scope=core) run is all that's stood between "partially supported" and a verdict (Tvashtr-38/39). NIM-quota-gated. M-ctx0 makes it more likely to land.
- **The deep-dive chain** (post-M-ctx0, each needs a migration `0019–0021`, freeze-bumped last): **M-ctx1** (context compiler + token budget + manifest + doc-handles) → **M-ledger** (trajectory ledger + the `RunEvent.seq` fix + the F1c run-inspector slot) → **M-tools** (D6: per-node MCP tools + skills) → **M-rails** (guardrail gate-nodes, on trigger).
- **Mv — the demand probe** (the real value gate, off the phase ladder): a non-founder user ships something real on their own repo. The operator's stance has been **build-first** (no probe until a feature-shipping Tvashtr exists — which it now largely is). Worth raising as an option, but it's the operator's strategic call whether the product is ready to probe.
- **Optional cleanups** (both §15, non-blocking, could be a quick data-less slice): app-wide **modal a11y** (focus-trap + Escape on the `.tv-scrim` pop-ups — `NewTeamDialog`, delete confirm, config drawer, `LaunchPanel`); the **residual non-MiniMap canvas test-warnings** (Background Dots + TeamNodePanel).

**`/goal` LENGTH CAP: 4000 characters** (a 5446-char F2c `/goal` was rejected; F3's was 3578, papercuts' 3189). Keep every `/goal` under 4000; lean on the `prompts/*.md` brief for granular detail (the `/goal` says "Follow prompts/<x>.md exactly"). Verify with `wc -c` before handing it over.

## 4. Standing operator directives (CARRY FORWARD — inherited every session)
- **Role = architect/planner ONLY.** All implementation (product code, diagnostic/throwaway scripts, build/Makefile/config) goes through a Claude Code `/goal`. Architect-direct edits are ONLY: the two living docs, `prompts/*.md` briefs, and trivial doc/typo fixes. When tempted to write code, write a `/goal`.
- **The disk audit is the main control point.** Never trust the self-report — read the changed files, byte-diff "untouched"/"additive-only" claims against a pre-image stashed to `/tmp/`, read test bodies to confirm they're mutation-real, and for a bug-fix confirm the reproduce-first regression genuinely goes RED on the old code. **Scrutinize EVERY deviation from the brief** — read the actual change and judge it (this session both F3 copy tweaks DROPPED over-promises = sound; the papercuts "test the mounted error path not a post-unmount no-op" call was sound). The **`git diff --stat main <branch>` / `git show --stat <sha>`** is the free scope proof (→ byte-intact set by omission).
- **No `git` CLI in the MCP** — reconstruct git state from `.git` plumbing: `.git/refs/heads/<branch>` (tip; a `feat/x` branch's ref is at `.git/refs/heads/feat/x`), `.git/logs/refs/heads/<branch>` (reflog → parent chain + FF-ability + nothing-pushed), `.git/logs/refs/heads/main` (main's whole history + how prior parallel branches merged). A worktree's branch ref lives in the MAIN repo's shared `.git/refs/heads/` (readable). **Operator is your git-CLI proxy** for a diff you can't compute — give ONE clean command (`git diff --stat main <branch> | cat` avoids the pager; plain `git show --stat` opens `less` → tell them to press `q`).
- **The copy-cache gotcha:** `copy_file_user_to_claude` caches by basename — a 2nd copy of a file you already copied returns the STALE version. Before re-copying a changed file, `mv`/`cp` the old container copy to `/tmp/` (that also gives you a pre-image to diff against). `Filesystem:read_text_file` is cache-free (but has only `head`/`tail`, no ranges — use the container copy + `sed` for mid-file ranges).
- **Editing the operator's disk:** `Filesystem:edit_file` (line-based; the returned diff is the verifier — use exact multi-line anchors; `dryRun:true` first for anything delicate) or `Filesystem:write_file` (full overwrite — used for the HANDOVER rewrite). `create_file`/`str_replace`/`bash_tool` write to CLAUDE's container ONLY (invisible to the operator).
- **Merges: operator FF-merges** (`git merge --ff-only`). ALWAYS give ALL merge commands as copyable text (cd, checkout, merge, verify line + expected sha, what-if-it-fails, branch cleanup). A parallel/worktree branch that DIVERGED merges by **cherry-pick** (`git cherry-pick <sha>` — the shas get REWRITTEN; then `git branch -D` FORCE, because a cherry-picked branch isn't a literal ancestor; + `git worktree remove --force <path>`). One step at a time (F3-merge, wait for "merged", then papercuts-cherry-pick).
- **Every Claude Code handoff = the COMPLETE copyable launch package, every time** (even if unchanged): (1) `cd /Users/adimac/Desktop/Tvashtr && claude --dangerously-skip-permissions`, (2) the FULL init prompt verbatim, (3) the FULL `/goal` verbatim (**under 4000 chars**). NEVER "same as before"; NEVER point them at a `prompts/` file for the init/`/goal` text. The detailed BRIEF lives in `prompts/<n>.md` (the `/goal` references it). For a parallel/worktree session, ALSO give the `git worktree add -b <branch> <path> main` command first + an init prompt that tells CC it's already on that branch in a worktree (do NOT `git checkout`).
- **`/goal` = one bounded milestone**: outcome + invariants-as-on-disk-evidence + the acceptance checklist CC runs ITSELF to green (make test/lint, FE build+vitest, live smoke, Playwright self-sign-off with a screenshot per check) + reproduce-first for bug-fixes + stop conditions (NEEDS_HUMAN + turn cap; distinguish "unknown/broad → stop" from "code-proven contained fix → proceed"). CC runs ALL verification — never hand the operator manual commands.
- **Design decisions: from the vision (§1) + the Tvashtr-25 pivot, never from effort. One at a time, explicit sign-off before the next. No option menus for vision calls — decide. Pair EVERY decision with its concrete user-facing UX consequence.** (Reserve "present the options and let me choose" for genuine strategic-direction calls — e.g. §3's next-track decision.)
- **Deferred items → §15 register IMMEDIATELY** (with the decision that deferred them).
- **Visual sign-off = a concrete, NUMBERED, click-by-click script** (name the exact team by rail label + the exact node by VISIBLE label + the exact action + explicit pass/fail). Default to CC's Playwright self-sign-off with a screenshot per check; the operator eyeball is the backstop.
- **Bypass mode caveat** (`--dangerously-skip-permissions`): allow/deny rules go INERT — only PreToolUse hooks survive. `protect-migrations.sh` (freeze) + `protect-no-push.sh` (no-push) are BOTH hooks, so they hold. The agent NEVER pushes; commits only its OWN changed paths (leaves the living docs + `prompts/*.md` for the architect).
- **Communication (operator preferences):** terse — "proceed"/"go"/"merged"/"done" = ratify + move on; "By the way" wants a short answer. **Procedures ONE step at a time.** **Analogies help. Simple, everyday language — avoid jargon.** Always hand copyable artifacts; never make them scroll back or reconstruct.
- **Session hygiene:** watch context; wrap up cleanly BEFORE running out (warn proactively). "handover" from the operator is an immediate trigger. Close out = PROJECTPLAN (§17 as-built + §15 register + §16 status + header bump) then a full HANDOVER rewrite then the ready-to-paste opener + the docs-commit command.

## 5. Parallel-session playbook (proven twice — Tvashtr-46 landing + Tvashtr-47 papercuts)
If the operator wants two Claude Code sessions again:
- **Give the file-overlap map FIRST and wait for approval** — name the exact files each session touches and where they collide, before any `/goal`. (Tvashtr-47 achieved ZERO shared files → the cleanest case.)
- **The ideal Session-B candidate is a FE-only, DATA-LESS, disjoint-file slice.** DATA-LESS is the key property — no backend/DB means zero shared runtime state, so it can't contend with Session-A's live Postgres/Playwright. (The papercuts session was vitest+build only — it didn't even need a vite port. A session that DOES run a dev server needs its own port, e.g. 5174.) It runs in its OWN `git worktree` off `main`.
- **Honor the three hard invariants INSIDE the session** (a merge can't fix them): (1) NO new migration — two branches each adding one produces two heads + a broken chain git won't flag; (2) executor (`team_run.py`/`graph_validity.py`) byte-intact; (3) minimize file overlap.
- **The overlapping/worktree session merges LAST** — it branched off the same base as Session-A, so once A merges (advancing `main`), B has DIVERGED → **cherry-pick** (NOT `--ff-only`), which REWRITES its shas → then `git branch -D` (force) + `git worktree remove --force`. `main`'s own history already shows this pattern (the F4 landing cherry-picks).
- **The worktree is OUTSIDE the architect's allowed filesystem dirs** (`/Users/adimac/Desktop/Tvashtr-*` is not readable) — you can't read its files directly. Verify its scope via the plumbing (its branch ref/reflog ARE in the shared `.git`) + the operator-proxy `git show --stat <sha>`, and do the CONTENT audit AFTER it lands in the main repo (post-cherry-pick, the files are in `main`'s working tree → readable).

## 6. Gotchas & tools
- **Commit the docs closeout** (the only dirty state to commit — includes this session's two briefs):
  ```
  cd /Users/adimac/Desktop/Tvashtr
  git add PROJECTPLAN.md HANDOVER.md prompts/F3-auth-wizard.md prompts/FE-papercuts.md
  git commit -m "docs(tvashtr-47): F3 auth wizard shipped -> M-frontend COMPLETE + FE-papercuts; handover to Tvashtr-48"
  ```
  (The deep-dive `.md` files + `design/` stay untracked. Leave `STATE.md` — it's the agent's spent F3 signal.)
- **PROJECTPLAN.md is ~250KB / ~900 lines.** NEVER read it whole. Copy it (`copy_file_user_to_claude`) → `grep -nE '^#{1,3} '` for a header index → `sed -n 'A,Bp'` targeted reads. Key sections: §1 vision, §6.4 the human-authored-graph constitution, §14 features, **§15 roadmap + deferred register**, §16 milestones, **§17 append-only as-built log**, §18 glossary.
- **The auth surface after F3:** `AuthGate.tsx` (top-level router: logged-out → `LandingPage` → CTA → `AuthWizard`; logged-in → `Dashboard` → open a team → `App` canvas) → **`AuthWizard.tsx`** (the guided wizard; `AuthMode` type lives HERE now, imported by `AuthGate`+`LandingPage`) → `Dashboard.tsx`. `LoginScreen.tsx` is GONE. The wizard reuses `api.ts` `login`/`register`/`AuthUser`/`ApiError` unchanged.
- **The seeded dev team is "My team"** = the `review_loop` template: visible node labels **Product manager** (entry), **Engineer**, **Reviewer** (workers), **PRD approval** + **Escalation** (gates), **Ship** + **Stop** (terminals). Bring-up: `make db-up && make migrate && make seed && make backend` + `make frontend` (Vite :5173); login `operator@tvashtr.local` / `tvashtr-dev`. (F3 registered a throwaway `f3+<ts>@tvashtr.local` in the dev DB — a `make seed`/DB reset clears it.)
- **Backend key facts:** `Run.id == Run.workflow_id == the DBOS workflow id` (set at creation) — so any `run_id`-keyed lookup and `DBOS.cancel_workflow(str(run.id))` are consistent. The run→library-team link is per-NODE (`AgentNode.cloned_from_node_id`), NOT a direct FK; `_run_rollup_by_origin` (F2b, `teams.py`) + `_latest_invocation_by_origin` (`routers.py`) both do this clone→origin join. The SHARED cancel core is `cancel_run_core` in `teams.py`; run-scoped tables (`cost_records` by `workflow_id`; `run_events`/`agent_invocations`/`human_tasks`/`engineer_run_attempts` by `run_id`) have NO FK cascade → an ordered multi-table delete. The executor is `backend/tvashtr/control_plane/team_run.py` + the graph-validity module (frozen — grep for them to prove byte-intact on any slice).
- **FE status is centralized in `frontend/src/lib/status.ts`** — the single source of truth for all derived status (`deriveNodeStatus`, `deriveGateState`, `deriveOverall`, `runStatusPill`). Extend it there, NOT inline in components. `StatusPill.tsx` is typed to the canvas `NodeStatus` vocab with fixed labels.
- **The design targets** live in `design/Tvashtr Frontend Overhaul/*.dc.html` (Claude-Design "DC" exports — inline-styled mockups using a fake `DCLogic` framework; TRANSLATE to real React + `.tv-*` CSS + lucide-react, they're the target LOOK not a rebuild). M-frontend used them all (`AuthWizard`/`Dashboard`/`Canvas`/`Landing`.dc.html); the token set (`design-system/tokens/*.css` + `extend.css`, imported by `index.css`) is complete — reskins add `.tv-*` classes to `index.css`, never new tokens.
- **Tools:** Filesystem MCP (operator disk — `read_text_file`/`read_multiple_files`/`edit_file`/`write_file`/`list_directory`/`list_directory_with_sizes`/`copy_file_user_to_claude`; load them via `tool_search` at session start), `bash_tool` (Claude's container ONLY — for files copied into `/mnt/user-data/uploads/` + diffs against `/tmp/` pre-images), Playwright MCP (automated UI/E2E inside a `/goal`). Briefs: `prompts/CLI-RULES.md` + `prompts/CLI-SETUP.md` (operating contracts — note CLI-RULES is somewhat stale on specifics like head/floors; the init prompt overrides them with current state).

## 7. Open questions / to-confirm
- **The next-track decision is Tvashtr-48's first task** (§3) — a strategic-direction call for the operator (present the options: M-ctx0 [+ paired brownfield re-run] / the deep-dive chain / Mv / the optional a11y+test-hygiene cleanups). M-frontend is DONE, so nothing is gated anymore.
- **§15 deferrals standing (non-blocking, none scheduled):** the F3 "what the onboarding answers DO" (evaporated for now — decide later); the auth-step BYOK fineprint claim (confirm it matches real key-storage); the residual non-MiniMap canvas test-warnings; app-wide modal a11y; durable run→library-team FK (needs a migration); orphaned PM `documents` on run teardown; per-run history drill-down. Plus the older register (C1–C9 keeps with triggers, X1–X13 rejects, per-node fallback model, typed-output/multimodal, hosted-OSS serving).
- **The frozen backend contract still holds until a migration is deliberately opened** — head `0018`, executor byte-intact. The first post-M-frontend slice that needs schema (M-ctx1 onward) creates a NEW migration `0019` and bumps the freeze LAST.

# HANDOVER — Tvashtr-9 → Tvashtr-10

> Structured snapshot to start the next chat. **Read this, then read `PROJECTPLAN.md` in full** (it's the source of truth; this file adds emphasis + what's not already there).
> **Written:** 2026-06-17 by Tvashtr-9.

---

## 0. Two standing rules that govern everything (absorb first)

**(A) Division of labor — the architect writes NO implementation.** The planning chat (Claude) is **architect/planner ONLY**: it designs, **writes the Claude Code prompt**, reviews the report, **verifies by reading the disk**, and maintains the two living docs (`PROJECTPLAN.md` + `HANDOVER.md`). **ALL implementation goes through a Claude Code prompt — product code, diagnostic/acceptance scripts (incl. `docker_smoke.py`/`containment_*.py`-style harnesses), AND `Makefile`/config changes alike.** The only direct edits the architect makes: the two living docs + trivial doc/comment/typo fixes. When tempted to write code, write a prompt instead. *(This corrects a drift that recurred twice — Tvashtr-8 and again Tvashtr-9; see the two 2026-06-16/17 §17 entries + §0 in PROJECTPLAN. There is NO "diagnostics are architect-direct" carve-out.)*

**(B) Working method — the prompt-based cycle (reverted Tvashtr-9).** We build with the **original prompt-based loop, NOT the `/tvashtr-loop` skill**. The cycle:
1. **Claude writes ONE self-contained Claude Code prompt in chat** — Objective / Context / Constraints / Tasks / Acceptance criteria / Report-back spec, with **test + lint runs required in the report-back**.
2. **Operator runs it** in Claude Code → it does the work + returns a report.
3. **Operator pastes the report back.**
4. **Claude audits the disk against the report** — reads every changed file; **never trusts the report over the disk**.
5. **Operator smoke-tests** (esp. the **visual/UI** check — this is his felt signal of progress) and **merges** (branch-per-step; operator merges).

Why the revert: running steps through `tvashtr-loop` *felt* slower/less productive and quietly sidelined the operator's per-step visual smoke-test (the loop's green gates became the de-facto sign-off). Also, much recent work was invisible plumbing, which feels unproductive under any process.

**Two tools stay available and are ENCOURAGED when useful (operator: "always productive") — the revert is NOT "tools bad":**
- the **`/tvashtr-loop` skill** (`.claude/skills/tvashtr-loop/SKILL.md`) — keep it on the shelf for a *purely mechanical* step where its automation earns its place;
- the **Playwright MCP** — Claude Code can use it for **automated UI/E2E testing inside a prompt**, **complementing** (not replacing) the operator's manual visual smoke-test. Lean on it for any UI-bearing step (P1.5+).

---

## 1. Where we are
**Phase 1 in progress. P1.1 (HitL), P1.2 (cost caps), P1.3a (Docker sandbox path), and P1.3b parts 1 + 2 are all COMPLETE & verified.** The Docker sandbox exists behind the `EngineAdapter` seam; crash-resume over the container is proven; the agent-server binds an **ephemeral** host port; and **forced-escape containment is now PROVEN (two layers)** — the write-escape that P0.4b demonstrated is empirically contained. **The default sandbox is still `local`.**

P1.3b is delivered in **three parts**:
- **Part 1 ✅ (Tvashtr-8): ephemeral host ports + crash-resume over the container.** Merged.
- **Part 2 ✅ (Tvashtr-9): forced-escape containment proof** (the inverse of P0.4b). Merged (see §3 — confirm). Two layers proven live: **least-privilege** (privileged out-of-`/workspace` writes denied in-container) + the **Docker boundary** (writable `/tmp`/`/var/tmp` writes land but are trapped in the `--rm` no-bind-mount container; host clean, only the deliverable ships).
- **Part 3 — flip the default** `agent_sandbox_mode` `local`→`docker`. **← TVASHTR-10'S JOB.** "Prove it, then default it" — part 2 proved it; part 3 defaults it.

**Tvashtr-10 picks up at part 3** (a one-line `config.py` change, decided by the architect because it's a one-way-door safety/security posture flip; design nuance in §5).

## 2. What P1.3b part 2 delivered (verified — code audit + offline gates + 2 live runs)
Built **via a Claude Code prompt** (the corrected division of labor): brief `prompts/P1.3b-part2-containment.md` → commit **`1c8220e`**, then a rev1 delta brief `prompts/P1.3b-part2-containment-rev1.md` (`5f732a1`) → commit **`05ae48e`**, on branch **`feat/p1.3b-part2-containment`**. Full detail + the vacuous-proof correction are in **PROJECTPLAN §17** (the 2026-06-17 part-2 entry). The shape:
- **Two net-new scripts (NOT in the pytest path; `make test` still 61):** `scripts/containment_smoke.py` (**Layer A** — deterministic, no-LLM; drives `OpenHandsDockerAdapter`/the container directly) and `scripts/containment_demo.py` (**Layer B** — a **real** `gpt-4o-mini` driven through `OpenHandsDockerAdapter.run()`, hostile instruction confined to the script). Two make targets: **`containment-smoke`** + **`containment-demo`**.
- **`control_plane/team_run.py` left UNTOUCHED** — the escape-attempting instruction lives only in the demo script (grep-confirmed zero hostile text in `team_run.py`); the DQ3 relative-path engineer instruction is unchanged. The only adapter change was **comment-only** (dropped the stale "free the host port" parenthetical — the part-1 nit).
- **The proof is TWO-LAYER, after a real correction (the key lesson):** the first live `containment-smoke` was **vacuous** — its three probe vectors were all *privileged* (`/escape_abs.txt`, `../escape_traversal.txt`, `mkdir /Users`), and the agent-server's **non-root user has those denied before the Docker boundary is ever exercised** — so nothing ever landed outside `/workspace` to test the boundary. The script's **non-vacuous guard correctly caught it** (exit 1). Fix (rev1, Option A): keep the three privileged vectors (they now evidence **least-privilege** when DENIED) **and add two writable vectors** (`/tmp`, `/var/tmp` — world-writable `1777`, outside `/workspace`) that **LAND** and are shown trapped; make every assertion **outcome-aware** (classify by the actual in-container `test -f` result, never by the `privileged`/`writable` *label*). New Layer-A PASS = `boundary_exercised ∧ keep_ok ∧ selectivity ∧ host_clean ∧ pull_target_clean`, where `boundary_exercised = (≥1 write actually landed outside /workspace)`.
- **Verified live (both green, container reaped, no orphan):** *Layer A* (`make containment-smoke`) — least-privilege DENIED `[absolute, traversal, host-path]`; boundary LANDED+contained `[tmp, var-tmp]`; pull = `['keep.txt']`; host clean. *Layer B* (`make containment-demo`) — the real agent **attempted all five** escapes (all 5 tokens in the event stream → non-vacuous), three privileged denied, `/tmp`+`/var/tmp` landed in-container, **yet `files_changed=['deliverable.txt']` only, host clean, critical host path not leaked**. The literal inverse of P0.4b.
- **One build-hygiene nit (→ §15, registered):** `make lint`/`make fmt` scope `ruff` to `backend/`, so `scripts/` (incl. all the diagnostic/acceptance scripts) sits outside the standing gate — a one-line Makefile change would fold it in. Low-risk.

## 3. What's in flight / loose ends
**Two doc commits + the part-2 merge are the pending git actions — Tvashtr-10 should confirm they landed before starting part 3.**
- **Part-2 merge.** The branch is `feat/p1.3b-part2-containment` (commits `1c8220e` + `5f732a1` + `05ae48e` + the part-2 PROJECTPLAN finalization). The operator was handed:
  ```
  git add PROJECTPLAN.md
  git commit -m "docs(p1.3b): part 2 containment PROVEN (two layers); finalize §17/§15"
  git checkout main && git merge feat/p1.3b-part2-containment
  ```
- **The Tvashtr-9 closeout docs** (the working-method-reversal §17 entry + this HANDOVER rewrite) are the architect's final edits this session — **uncommitted on `main`**; the operator commits them on `main` before opening Tvashtr-10 (e.g. `git commit -am "docs: revert working method to prompt-based; Tvashtr-9→10 handover"`).
- **Tvashtr-10: confirm** `git log --oneline` on `main` shows the part-2 commits + both docs commits, and `git status` is clean. *(Optional tidy: `git branch -d feat/p1.3b-part2-containment` and the other merged branches `feat/p1.2-cost-caps`, `feat/p1.3a-docker-sandbox`, `feat/p1.3b-crash-resume`.)*
- **No zombie runs, no pending migration.** Nothing else loose.

## 4. Immediate next steps (Tvashtr-10) — in order
1. **State your name** ("This is Tvashtr-10"), **read both docs** (`HANDOVER.md` + `PROJECTPLAN.md` in full); call `Filesystem:list_allowed_directories` to confirm the path (`/Users/adimac/Desktop/Tvashtr`, capital T).
2. **Confirm the part-2 merge + the two docs commits landed** (§3). Don't start part 3 on an un-merged/un-committed base.
3. **Design part 3 (the default flip) with the operator** — one design point at a time (operator style, §10). The core question: **flip now (close out P1.3b) vs defer the flip to P1.5** (when real multi-file code actually runs). Architect's lean: **flip now** (safety-by-default, like the $5 cap — containment is proven, so make it the default), **but keep the fast dev demos explicitly `local`** so trivial orchestration tests don't pay the ~3s heavy-container tax. Weigh with the operator (§5).
4. **Write ONE Claude Code prompt** for part 3 (the prompt-based cycle, §0-B). It is a **one-line `config.py` change** (`agent_sandbox_mode` default `"local"` → `"docker"`) **plus** pinning the fast dev demos to `local` (Makefile/script env) so they don't regress, **plus** the acceptance: prove the global default is now `docker` (a run with no override containerizes) and the `local`-pinned demos still run fast. Acceptance/report-back must include `make test` + lint + the relevant live targets the operator will run.
5. **Operator runs it → paste report → audit the disk → operator smoke-tests + merges → finalize §15/§17.** That **closes P1.3b**.
6. **Then P1.4** (the LiteLLM proxy) — see §5b for what's next.

## 5. Part 3 (the flip) + what's after — design before the prompt

### (a) Part 3 — flip the default to `docker`  ← TVASHTR-10'S FOCUS
One line in `config.py`: `agent_sandbox_mode` default `"local"` → `"docker"`, **now that part 2 has proven containment.** ⚠️ **This is a posture-setting default (safety/security) — a one-way door; the architect decides it.** **The nuance to settle with the operator:** flipping the *global* default means every dev run (incl. `skeleton-run`/`hitl-demo`/`budget-demo`) would spin the heavy agent-server container (~3s bring-up + VSCode/VNC) for trivial tasks that don't need containment until P1.5's real-code loop. **Clean resolution (architect's lean):** default = `docker` (safety-by-default), **but keep the fast dev demos explicitly `TVASHTR_AGENT_SANDBOX=local`** (they test orchestration, not containment); the docker path stays exercised by `skeleton-run-docker` + `skeleton-crash-docker` + `containment-*`. Decide: **flip now** (end of P1.3b) **or defer to P1.5** (when real code first runs). Either is defensible; flipping now matches the "prove it, then default it" framing and the safety-by-default posture.

### (b) What's after P1.3 (the visible payoff is close)
- **P1.4 — LiteLLM proxy:** the agent-internal spend chokepoint that cuts a runaway agent off **mid-loop**, closing the gap P1.2's between-steps enforcement can't. Small, still invisible.
- **P1.5 — the multi-node cyclic sprint loop + per-loop alignment check** (proves M1; likely 2–3 prompts): a graph-driven cyclic work/review executor with enforced termination (loop caps + gate conditions), a richer **hardcoded** team-builder (PM→Tech Lead→FE/BE), a simply-persisted canonical idea, an alignment check vs that idea → blocker on mismatch (via P1.1), the human gate. **This is the first genuinely VISIBLE feature again** — the canvas lighting up with multiple nodes cycling, real multi-file output streaming into the feed — i.e. where the operator's visual smoke-test (and the Playwright MCP) come back to the center, and the product starts to feel alive. It's also the reason the sandbox exists, so it lands on the now-proven containment.

## 6. Standing conventions (load-bearing)

### How we work (the prompt-based cycle — see §0-B)
- Default cycle: **Claude writes one tight, self-contained Claude Code prompt in chat** → operator runs it → pastes the report → **Claude audits the diffs against the disk** (read every changed file; never trust the report/scrollback over the disk) → **operator visual-smoke-tests + runs live targets + merges**. Branch-per-step; operator merges (after the architect finalizes §15/§17).
- **The prompt carries — and BE THOROUGH: make every prompt detailed and near-exhaustive, leaving nothing ambiguous (operator directive, Tvashtr-9; err toward MORE detail, not less):** Objective; Context (where it fits, **exact file paths + current code state + relevant snippets**); Constraints / hard-rails + an explicit **do-NOT-touch list** + the patterns/conventions to follow; Scope IN/OUT; **granular ordered Tasks** (exact commands, function/variable names, anchors); **Acceptance criteria** — the **specific** tests to write/run + expected outcomes + edge cases; **Report-back spec** — files changed, commands run **with output (incl. `make test` + lint)**, deviations, open questions, suggested next step. Prefer several tightly-scoped prompts over one sprawling one (P1.1 a/b/c, P1.3 a/b, P1.3b in 3 parts). Worked brief templates that double as prompt models: `prompts/P1.2-*.md`, `prompts/P1.3a-*.md`, `prompts/P1.3b-crash-resume-ephemeral-ports.md`, `prompts/P1.3b-part2-containment.md` (+ its `-rev1`) — the last few show how to write **explicit assertions for something the run-outcome alone can't prove** (part 1's orphan assertions; part 2's non-vacuous + outcome-aware boundary checks).
- **Live targets are the operator's** — anything needing real Docker + a real LLM (the `*-docker`, `containment-*`, `*-demo` targets) the operator runs; offline gates (`make test`/`lint`, `npm run build`/`test`) go in the prompt's report-back and the architect re-checks them against the disk.
- **Available accelerators (use when they help):** the `/tvashtr-loop` skill for a purely mechanical step; the **Playwright MCP** for automated UI/E2E checks inside a prompt (esp. P1.5+ UI work).
- **Decision log over silent revision** — when the operator pushes back or direction changes, record the reasoning in §17 (both the part-1 hand-editing correction and the Tvashtr-9 working-method reversal are recorded there). **Operator-flagged "don't forget" items → §15 register immediately.**

### The hard rails (every prompt must honor these)
- **Boundary discipline:** only `gateway/gateway.py` imports `litellm`; **only the two OpenHands adapters import `openhands.*`** — `engines/openhands_adapter.py` (local) + `engines/openhands_docker_adapter.py` (docker), both at module top, with **laziness via `engines/registry.py`** (imports an adapter only when `resolve_adapter(name)` runs). App startup + the offline suite stay openhands-free; **a test enforces it** for the registry, `docker_runtime`, and `team_run`. `docker_runtime.py` (startup path via the boot sweep) imports **only** stdlib + `tvashtr.config` — never `openhands.*` or the `docker` Python lib (installed transitive dep, deliberately unused; reaping shells out to the CLI).
- **At-least-once-safe writes:** every side-effecting write in a `@DBOS.step` is idempotent on a `DBOS.workflow_id`-derived key (`{run_id}:pm-prd-v1`, `{run_id}:agent-cost`, the `ship-{run_id}` git tag, `(run_id, seq)` events, gate `(run_id, topic)`, budget `(run_id, topic)`). `workflow_id == run_id == str(Run.id)` via `SetWorkflowID`.
- **The gate primitive is reused:** PRD gate (P1.1), budget-breach blocker (P1.2, via `enforce_budget`) — each `wait_at_gate` → `DBOS.recv` resumed by `DBOS.send`. The kill switch (`DBOS.cancel_workflow` + `Run.status="cancelled"`, excluded from PENDING-only recovery) is the interrupt path **and** the way to retire a zombie run.
- **Running cost is a query** (`metering.running_cost(run_id)`), not a stored field; `Run.cost_total_usd` is finalize-only.
- **Frontend:** consume the Design System from **token CSS** (**no ad-hoc hex** — tokens or `color-mix`). `status.ts::deriveNodeStatus`/`deriveOverall`/`isRunTerminal` = the **single source of truth** for derived status (vocabulary `idle/running/paused/done/stopped/failed`; terminal set incl. `over_budget`). **Polling (~1.8–2s)** is the transport (WebSocket = named **P1.6** upgrade). The **canvas is read-only** (authoring is Phase 2). FE gates: `npm run build` (strict `tsc` + `vite`) + `npm test` (vitest). *(Part 3 is config/Makefile-only — no FE change.)*
- **Verify fast-moving SDKs against the installed package** (DBOS vs `.venv/.../dbos/`; OpenHands vs `backend/.venv/lib/python3.12/site-packages/openhands/`) — P1.3a corrected three brief assumptions this way; part 1 confirmed `DockerWorkspace(host_port=None)`/`find_available_tcp_port`; part 2 confirmed the container's non-root permission model the hard way. **And verify reports against the disk.**
- **Single `OPENROUTER_API_KEY`** for both the gateway and the agent's internal LiteLLM (the key rides in the **agent config**, serialized to the server — *not* the container env). `.env` gitignored; no key in any commit; repo has no remote.
- **UI bar = "n8n-level (or better)";** the operator's **live eyeball** is the visual gate. Make trivial doc/comment fixes directly; **all implementation (incl. scripts + config) goes through a Claude Code prompt** (§0-A).

## 7. Open risks & carried items (PROJECTPLAN §13 + the §15 register)
- **✅ Write-escape containment is now PROVEN (no longer the top open item).** Part 2 confirmed empirically, on two layers, that a real escape-attempting agent is contained over the container (host clean, only the deliverable ships). The relative-path prompt instruction is now just a happy-path convenience, **not** the boundary — the container is. **Remaining close-out:** the global default is still `local`; **part 3 flips it** (then real-code execution in `docker` is the safe default).
- **Image-based reaping is all-or-nothing** (no per-run targeting) — fine for serial single-operator, **breaks under concurrency**. Registered §15 (the ephemeral-ports half is DONE). Trigger = **P1.5's parallel nodes** or multi-project.
- **Image version skew** — `latest-python` banners SDK **1.28.0** while the host pins **1.28.1**; protocol worked across it, but the moving tag hurts reproducibility (§15: pin a digest).
- **Budget enforcement is reactive / between-steps (P1.2, by design):** a breach can overshoot ~one step's spend; the agent's own mid-step spend can't be interrupted until **P1.4 (the LiteLLM proxy)**.
- **`scripts/` is outside the lint/format gate** (NEW, part 2 — §15): `ruff` is scoped to `backend/`, so the diagnostic/acceptance scripts aren't enforced. Low-risk; one-line Makefile fix when convenient.
- **Other carried items (§15):** the `bash_events`/`conversations` **scaffolding denylist is pragmatic and brittle** (relocate the server's stores is the principled fix); the FE component/lifecycle-tests gap (vitest is pure-function only); the per-run resolve-suppression edge; the cancelled-gate blocked-`recv` lingering up to 3600s; verbose run-event payloads; **`backend/.tvashtr_workspaces/<run_id>/` accumulates with no GC**; in dev **DBOS resumes abandoned `running`/PENDING runs on every backend boot** (the M0 property + why the kill switch exists).
- **Named upgrades parked in §15 (do NOT drop — operator-flagged):** agent-native resume (reconnect to a surviving container instead of restarting — its enabler exists: the container outlives the kill-9'd client); relocate the Agent Server event stores; pin the image digest; **per-run container targeting**; per-run sandbox-mode observability; per-project budget cap; cost-threshold alert; richer budget-breach resolution; the **LiteLLM proxy (P1.4)**; **section-level** then **CRDT/Yjs** doc locks; **WebSocket (P1.6)**; the **Tasks-for-Human drawer (P1.5)**; FE component tests; workspace GC.

## 8. Tech stack (all LOCKED — PROJECTPLAN §8)
DBOS Transact 2.23.0 · LiteLLM 1.89.0 · React Flow `@xyflow/react` 12.11.0 · **OpenHands SDK 1.28.1** (`openhands-sdk` + `openhands-tools` + `openhands-workspace` for `DockerWorkspace`) · the **OpenHands Agent Server image** `ghcr.io/openhands/agent-server:latest-python` (pulled; heavy — VSCode/VNC baked in; ~3s bring-up; **banners SDK 1.28.0**, a §15 digest-pin item; runs as a **non-root user that cannot write the container `/`** — the least-privilege layer proven in part 2) · Postgres 16 (port 5433) · FastAPI (8000) · SQLAlchemy 2 + Alembic · Python 3.12 / uv · Vite 7 / React 19 / Tailwind v4 (5173) · `lucide-react` 1.18.0 · vitest 3.2.6 · the operator's **Design System** (vendored tokens). **Sandbox modes:** `local` (in-process LocalWorkspace, **still the default**) and **`docker` (Agent Server container — built + verified P1.3a; crash-resume + ephemeral ports P1.3b part 1; containment P1.3b part 2; becomes the default after part 3)**. **`agent_server_host_port` defaults to `None` (ephemeral)** — an int override pins a port. **Tooling available for prompts:** the **Playwright MCP** (UI/E2E checks) + the `/tvashtr-loop` skill (mechanical steps). **Coming in Phase 1:** the **LiteLLM proxy** (P1.4) · **TipTap** (P1.7) · **WebSockets** (P1.6).

## 9. Repo state & how to drive it
- **Project root:** `/Users/adimac/Desktop/Tvashtr` (Claude Code runs here; the architect chat edits the living docs here directly via Filesystem MCP).
- **Build/branching:** a **feature branch per step**; the **operator merges** to `main`. P1.3b part 2 = `feat/p1.3b-part2-containment` — **confirm it's merged** (§3); delete it + the other merged branches after.
- **Make targets:** `setup`, `db-up`, `db-down`, `migrate`, `backend`, `frontend`, `test`, `lint`, `fmt`, `smoke`, `agent-smoke`, `skeleton-run`, `skeleton-run-docker` (`TVASHTR_AGENT_SANDBOX=docker`), `skeleton-crash`, `skeleton-crash-docker`, **`containment-smoke`** (Layer A, no-LLM), **`containment-demo`** (Layer B, real agent), `crash-demo`, `hitl-demo`, `budget-demo`. Live targets skip cleanly without `OPENROUTER_API_KEY`; the docker/containment ones need Docker + the agent-server image and take a few minutes. **The UI is `make backend` + `make frontend` → `http://localhost:5173`.** *(Part 3 will likely pin `skeleton-run`/`hitl-demo`/`budget-demo` to `local` and let the global default carry `docker`.)*
- **Diagnostic scripts:** `scripts/docker_smoke.py` (no-LLM container plumbing smoke + reap-lifecycle phase); `scripts/containment_smoke.py` + `scripts/containment_demo.py` (the part-2 two-layer proofs); `scripts/skeleton_crash_demo.sh` + `scripts/skeleton_crash_demo_docker.sh` + `scripts/check_skeleton_crash.py` (mode-agnostic durable-outcome checker). **All written via Claude Code prompts, not architect-direct.**
- **Migrations:** `0001`–`0007`; **`0007_run_budget_caps` is newest** (P1.2). P1.3a/b added NONE. Next is `0008`.
- **Prompts/briefs in `prompts/`:** `P0.1`…`P0.5b`, `P1.1a/b/c-*`, `P1.2-cost-caps-budget-enforcement.md`, `P1.3a-containerized-execution-path.md`, `P1.3b-crash-resume-ephemeral-ports.md`, **`P1.3b-part2-containment.md`** + **`P1.3b-part2-containment-rev1.md`**. Add `P1.3b-part3-default-flip.md` (or write the part-3 prompt straight in chat — operator's preference; both are fine now).
- **HTTP surface (`routers.py`):** `POST /api/runs` → `{run_id}` (optional `budget_cap_usd`); `GET /api/runs/{id}`; `GET /api/runs/{id}/graph`; `GET /api/runs/{id}/tasks` + `POST …/tasks/{task_id}/resolve` + `POST …/cancel`; `GET /api/documents/{id}`; `GET /api/spike/run-events/{id}` (`{run_id, events:[{seq,kind,payload,created_at}]}`). **Run `status`: `pending|running|awaiting_human|completed|failed|rejected|cancelled|over_budget`.**
- **Backend:** `control_plane/{team_run.py (coarse agent step + mode selection + the DQ3 engineer instruction + idempotent ship), gates.py, budget.py, shipping.py, teams.py, doc_writer.py}`; `gateway/`; `metering.py`; **`engines/{base.py (contract), openhands_adapter.py (local), openhands_docker_adapter.py (docker), docker_runtime.py (reaping), registry.py, run_event_sink.py}`**; `models.py`; `config.py` (`Settings` incl. **`agent_sandbox_mode="local"`** ← part 3 flips this, `agent_server_image`, `agent_server_host_port=None`, `agent_server_platform`, `default_run_budget_usd=$5`, `default_max_tokens_per_call=4096`); `main.py` (boot-sweep `lifespan`). Tests: `tests/test_{docker_adapter,docker_runtime,config_sandbox,registry,budget,gates,shipping,...}.py` (61 total).
- **Frontend:** `lib/` (`api.ts`; `status.ts` = derived-status SoT; `events.ts`; `status.test.ts` + `events.test.ts`), `canvas/`, `panel/`, `components/` (StatusPill, RunBanner, BackendDot, TasksForHuman, CancelRunButton), `design-system/`, `index.css`/`canvas.css`/`panel.css`.
- **Commit state:** part 2 awaiting merge + the two Tvashtr-9 docs commits awaiting commit on `main` (§3); otherwise `main` has P1.1+P1.2+P1.3a+P1.3b-part1.

## 10. Operator working style (apply throughout)
- **Analogies help** when explaining concepts.
- A message starting with **"By the way"** = give a **short** answer.
- **Procedures one step at a time** — deliver step 1, wait for the reported result, then step 2 (governs commands, installs, live checks; a checklist of *independent* verifications can be given together).
- **Design questions one at a time too** — ask one, wait for the answer, then the next.
- **We build via the prompt-based cycle** (§0-B): one **tight, self-contained Claude Code prompt per step**, written in chat; the operator runs it; the architect **audits the diffs against the disk**; the operator **visual-smoke-tests + merges**. Prefer several tightly-scoped prompts over one sprawling one. The `/tvashtr-loop` skill + Playwright MCP stay available when useful.
- **Decision log over silent revision** — record direction changes + pushback reasoning in §17.
- **Operator flags things he doesn't want forgotten** — capture them in the §15 register / §17 immediately.
- **UI/UX quality matters — "n8n-level (or better)";** the operator's live eyeball is the visual gate (the reason we reverted off the loop).
- **Chat naming:** open by stating your name ("This is Tvashtr-10"); the number comes from the first message of the chat.

## 11. Pointers
- **Source of truth:** `PROJECTPLAN.md` — esp. §0 (division of labor), §6 architecture, §7 decisions (D1–D10), §8 stack (locked), §9 data model, §13 risks, §15 roadmap + the **Deferred refinements register**, §16 milestones (**M0 ✅; P1.1 ✅; P1.2 ✅; P1.3a ✅; P1.3b parts 1 + 2 ✅**), §17 decision log (every P0.x/P1.x decision + verification — incl. **P0.4b** (the escape part 2 inverts), the **Tvashtr-8** part-1 entry, the **Tvashtr-9** part-2 entry, and the **Tvashtr-9 working-method-reversal** entry).
- **Worked prompt/brief templates:** `prompts/P1.2-*.md`, `prompts/P1.3a-*.md`, `prompts/P1.3b-crash-resume-ephemeral-ports.md`, `prompts/P1.3b-part2-containment.md` (+ `-rev1`).
- **Available accelerators:** `.claude/skills/tvashtr-loop/SKILL.md` (mechanical steps) + the Playwright MCP (UI/E2E checks).
- **The code part 3 touches:** `config.py` (`agent_sandbox_mode` default) + the Makefile/scripts for the `local`-pinned fast demos. The containerized path it makes default: `engines/openhands_docker_adapter.py` (+ `docker_runtime.py` reaping) — now proven on all three of crash-resume, ephemeral ports, and containment.
- **The Phase-0 closeout narrative:** `docs/M0-walkthrough.md`.

# Tvashtr — HANDOVER (Tvashtr-36 → Tvashtr-37)

## Read this first
You are **Tvashtr-37**, the architect/planner for Tvashtr (a Python/React web canvas for composing + running custom AI agent teams that take a product idea to working software). Read this file **and** the named `PROJECTPLAN.md` sections (§1 vision · the two 2026-06-28 Tvashtr-36 §17 entries · §15/§16) **and** `prompts/CLI-RULES.md` before any work. **You are ARCHITECT/PLANNER ONLY** — all implementation goes through a Claude Code `/goal`; your only direct edits are the living docs (`PROJECTPLAN.md`, `HANDOVER.md`), `prompts/*.md` briefs, and trivial doc/typo fixes. **The disk audit is your real control point** (never trust Claude Code's self-report).

## Where we are (current state)
- **main @ `28c2ce3`** · **alembic head `0017`** · **migration freeze `0001`–`0017`** (regex `^00(0[1-9]|1[0-7])_.*\.py$` in `.claude/hooks/protect-migrations.sh`) · **test floors 309 backend pytest / 159 vitest** · **14 tables** (Slice A added `User`; Slice B added `ProviderCredential` + `owner_id` on `runs` & `team_graphs`).
- **The M-accounts milestone: Slices A + B are SHIPPED + merged.** Tvashtr is now **fully account-based and `.env`-free for provider keys.** Logged-out → a **landing page** (no canvas). Login → a **dashboard** (the user's teams · previous runs · providers w/ add+remove). Open a team → the **canvas** (with back-to-dashboard). Every run is **owned by construction**; per-owner keys resolve from the **encrypted DB (Fernet) on BOTH the completion + agent paths — no `.env` fallback**; the seed imports the operator's `.env` keys ONCE + backfills, so the operator can **delete the provider keys from `.env`**. Full as-built: the **2026-06-28 (Tvashtr-36) Slice B** §17 entry.

## Immediate next step — M-accounts Slice C
**The per-node MODEL PICKER inside the canvas** (the operator explicitly wants it; it was deferred from Slice B only for size) + **recommendation hints**. The user picks the model per node from THEIR configured providers; Tvashtr recommends (e.g. "a heavier reviewer than your worker"), never enforces. **Substrate already exists:** `node.model` resolves end-to-end (`run_graph` → `AgentTask`/`CompletionRequest`); `reviewer_model()`/`engineer_model()` are separate seams; the dashboard provider endpoints (`GET/POST/DELETE /api/providers`) are live; the per-owner resolver maps `provider = model.split("/")[0]`. **Design it one-question-at-a-time, paired with its UX consequence, then a bounded `/goal`.** Open design Qs for the operator: (a) does the picker list only providers the account holds keys for, or all known providers with an "add key" affordance for missing ones; (b) where the recommendation hints surface (per-node in the authoring panel vs a team-level advisory); (c) a node with no model set → a default, or flagged un-runnable (it interacts with the `create_run` pre-flight 422). Slice C is FE+backend, **NIM-independent** (like A/B).

## Also pending (operator action, NOT a milestone) — the §9 `.env`-free verification
The payoff of Slice B. The operator runs (ORDER MATTERS): `make migrate` → `make seed` (imports the `.env` keys **while they're still present**) → **DELETE the provider keys from `.env`** → restart → log in → the dashboard shows the seeded providers → open a team → launch. The **`.env`-free PROOF** = the dashboard showing providers + the run **creating** (the pre-flight passes → owner has DB creds) + the **PM (completion)** producing a PRD from a DB-resolved key — NOT a clean agent completion (the Engineer/agent run is **NIM-window-dependent**; NIM is degraded). Keep **`TVASHTR_SECRET_KEY` stable** across seed→run (the dev default is fine; the stored keys only decrypt with the same Fernet key). A numbered script was handed to the operator at the Slice-B closeout; re-derive it if asked.

## Deferred / open (in or for §15)
- **DB-level `NOT NULL` on `runs.owner_id`** — later hardening (app-enforced + the executor hard-errors on NULL for now).
- **BYOK with the LiteLLM proxy ON** — the proxy holds upstream keys in its own config; per-owner upstream keys when proxy-ON is a later reconciliation (proxy is OFF by default; the proxy-ON routing is byte-unchanged).
- **Rung 2 (brownfield on a real OSS repo) is PARKED on NIM** — resume when NIM recovers; the confirmed **stronger-reviewer-model (D4)** lever comes due there (paid/hosted layer).
- The standing **Mv value gate** (a non-founder user ships something real + reaches for "reshape the team" unprompted) remains the real validation, gated behind M-brownfield.

## Gotchas / mechanics
- **No git CLI in the MCP** — reconstruct git from `.git/refs/heads/<b>` + `.git/logs/refs/heads/<b>` (the reflog shows the create→commit→amend chain + the branch-point parent → FF-ability + that nothing was pushed). **The OPERATOR runs all commits/merges (FF-only)**; you hand COPYABLE command blocks.
- **File access:** the **Filesystem MCP** is the mechanism for the user's machine files (read large files via `head`/`tail` — there is no arbitrary-range read; `Filesystem:edit_file` with `dryRun:true` FIRST + exact multi-line anchors). Some sessions also expose a `bash_tool` on Claude's own container (`/mnt`, NOT the project) for grep/diff; the audit can be done entirely via Filesystem reads if `bash_tool` is absent.
- **PROJECTPLAN.md is ~130KB** with a giant single-line header (line 6) — NEVER read it whole; use `head`/`tail` or known section anchors. §17 entries are appended right before `## 18. Glossary`.
- **The conftest `client` fixture now seeds 5 dummy provider creds + owns its runs** (`auth_user_id()`), so any NEW test that creates a run must use an OWNED run with credentials (the LLM is mocked → the dummy key is never used); a keyless owned run hits the `create_run` 422.
- **NIM degraded** — the live agent targets (loop-feature-docker, brownfield-check, the §9 agent run) may throttle; the goal-condition has a **4000-char limit** (keep the `/goal` under it, point at the brief).

## Standing operator directives (carry forward — these define HOW to work)
- **Architect/planner ONLY; ALL implementation via a Claude Code `/goal`.** Diagnose bugs yourself (read code/logs/git) but the FIX goes through a `/goal` — there is NO "diagnostics are architect-direct" carve-out.
- **The Claude Code launch package is ALWAYS fully copyable inline, EVERY time:** (1) the shell launch (`cd /Users/adimac/Desktop/Tvashtr && claude --dangerously-skip-permissions`), (2) the FULL init prompt verbatim, (3) the FULL `/goal` verbatim. NEVER "same as before"; NEVER tell the operator to fetch the init/`/goal` text from a file (the detailed brief MAY live in `prompts/*.md` and the `/goal` references it).
- **One design question at a time, decided directly (no option menus for vision calls), each paired with its concrete frontend/UX consequence, explicit sign-off before the next.** Decide from the vision (§1) + the Tvashtr-25 prompt-driven pivot, NEVER from effort-minimization. If the operator asks "are you deciding from the vision and not shying from work?" — that's the calibration signal.
- **The disk audit is the control point** — diff changed files against pre-images, read test bodies for mutation-reality (they must fail on pre-fix code), verify git from plumbing, scrutinize every deviation from the brief by reading the actual change.
- **`/goal` scope = ONE bounded milestone** (outcome + invariants-as-checkable-evidence + acceptance/evidence incl. the live targets CC runs to green + reproduce-first for bugfixes + stop conditions). CC echoes each gate's decisive line into the transcript (the `/goal` evaluator reads only the chat).
- **Merge handoff = ALL commands every time** (cd · checkout main · merge --ff-only · verify · optional `git branch -d`) + the expected tip SHA + "if FF fails, stop — that's divergence."
- **Visual/manual sign-off = a DETAILED numbered click-by-click script** (exact team/screen; the node by its VISIBLE label; the exact action; explicit pass/fail) — never an abstract checklist. CC's Playwright self-sign-off (with screenshots) covers the visible surface; the operator's eyeball is the optional glance.
- **Operator comms are TERSE:** "proceed"/"go"/"merged"/"done" = ratify + continue; "By the way" = a short answer wanted. Procedures **one step at a time** (give step 1, wait for the result, then step 2). Analogies help. Always hand over **copyable artifacts** (never make the operator scroll back or reconstruct).
- **§15-deferred discipline:** deferred items go to the §15 register immediately (with the deferring decision) so they can't silently vanish.
- **Guardrails (survive `--dangerously-skip-permissions`):** the no-push PreToolUse hook + the migration-freeze PreToolUse hook (allow/deny rules do NOT survive bypass). A new migration is `0018`+ (then bump the freeze regex LAST, only if the slice adds one). The agent never pushes; the operator FF-merges.

## The next-chat first message (give this to the operator)
> "You are Tvashtr-37. Read `HANDOVER.md` and `PROJECTPLAN.md` at the project root first (§1 vision + the two 2026-06-28 Tvashtr-36 §17 entries + §15/§16), then `prompts/CLI-RULES.md`; then pick up at **M-accounts Slice C** (the per-node model picker in the canvas + recommendation hints). Don't start work until you've read all three."

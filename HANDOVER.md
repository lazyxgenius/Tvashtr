# HANDOVER — Tvashtr-42 → Tvashtr-43

**You are Tvashtr-43**, the architect/planner chat for Tvashtr. Read this file **and** `PROJECTPLAN.md` (the line-6 header banner, §1 vision, §14, §15, §16, and the newest §17 "2026-07-02 (Tvashtr-42)" entry) before doing any design work. Then read `prompts/CLI-RULES.md` (the CC operating contract, incl. §4.7). You are ARCHITECT/PLANNER ONLY — design, write CC `/goal`s + `prompts/` briefs, audit results on disk, maintain the living docs. All implementation goes through Claude Code.

---

## Where we are (ground truth)
- **`main` @ `b33bb23`** — the Tvashtr-42 F1b docs-closeout commit, sitting on top of the F1b merge `937d27d`. Alembic head **`0018`**. Test floors: **328 backend pytest / 172 vitest** (F1b raised vitest 167→172).
- **Active milestone: M-frontend** — a premium reskin of the existing warm cream-paper frontend (KEEP-WARM identity: cream `#faf9f5` + coral `#d97757` + Newsreader/Inter/IBM-Plex-Mono; NO dark pivot), the canvas emulating **n8n's advanced AI-agent canvas**. Decomposed **F0 → F1 → F2 → F3 → F4**, reskinned onto the live/tested app. The design export is the target LOOK, not a rebuild. **The backend contract is the WALL** (no new endpoints, `api.ts` byte-untouched, no migration past `0018`).
- **Ordering LOCKED: canvas-first.** **F1 split into three slices: F1a (node cards) ✅ → F1b (canvas chrome + edges/handles + authoring affordances) ✅ → F1c (config drawer + drawer↔modal toggle + model picker + run-view) — NEXT.**
- **SHIPPED + merged + committed:** **F0** (`b43a0d0`), **F1a** (`e861b4c`), **F1b** (`937d27d`) — all disk-audited and clean; the F1b docs closeout is committed (`b33bb23`).
- **The whole M-frontend revamp is NOT done until F4.** After F1c: **F2** (dashboard), **F3** (auth wizard), **F4** (landing). The operator wants a **bold, all-caps "revamp finished" announcement only when F4 lands** — do NOT declare it done before then.
- **Brownfield thesis verdict: UNCONFIRMED + DEFERRED** — do NOT queue it. **Wizard-of-Oz demand probe (≥1 non-founder paying user): still the open value gate.**

## What F1b shipped (so you know what F1c inherits)
F1b = the canvas chrome (board surface + dotted background + bottom-left zoom/fit controls + bottom-right minimap), the edges/handles (forward/branch/reject/escalation + the `ReworkEdge` loop-back arc + the coral `tv-flow` animation on active edges + the coral dashed drag-to-connect ghost + connect dots), and the n8n inline authoring affordances over the existing CRUD:
- **The inline "+"** is **node-anchored** (RATIFIED — NOT n8n edge-splice; our edges carry roles/conditions that don't cleanly split): hover a **thinker/worker** node → a "+" → a kind-picker (`NodePicker.tsx`) → a new node to the right + a `role:"forward"` edge + auto-select (which opens the EXISTING side panel).
- **Hover-trash** on nodes (all kinds) + on edges (at the path midpoint, nudged above the label — the operator's edge-delete placement tweak).
- **The palette** (`NodePalette.tsx`) relocated to a top-left "Add to canvas" "+" popover, both groups kept (Add: Thinker/Worker/Gate/**Ship**/**Stop** — kept SEPARATE, since terminal-kind editing is F1c's — + Presets: PM/Architect/Engineer/Reviewer), menu shared via `paletteItems.ts`.
- New files (all `frontend/src/canvas/`): `WorkEdge.tsx` (a custom edge type carrying the hover-trash), `NodePicker.tsx`, `authoringContext.ts` (threads the affordance callbacks + editable/hover state into the node card), `paletteItems.ts` (the shared add-menu). `App.tsx` gained `handleAddDownstream`. Full detail in §17 (Tvashtr-42 entry).

## The design export (already reviewed + re-verified — reuse it, don't re-review)
- Lives at `design/Tvashtr Frontend Overhaul.zip` (2.22 MB on disk, modified 2026-07-01). **Re-verified byte-identical this session** — there are NO hidden newer canvas changes. Extract to a scratch dir OUTSIDE the repo (copy it over, unzip in `/tmp`).
- Its base tokens are byte-identical to the live tokens; the only new token material is **`tvashtr-extend.css`** (F0 ported it) — which ALREADY contains F1c's tokens/keyframes: **`--drawer-w: 384px`, `--shadow-drawer`, `tv-modal-in`** (+ `tv-pop-in`, `tv-flow`, warm scrollbars, reduced-motion). Consume them; don't redefine.
- The `.dc.html` mockups style everything with inline token-recipes (no CSS classes). **Canvas mockup = `Canvas.dc.html`.** Other surfaces (F2–F4): `Dashboard.dc.html`, `AuthWizard.dc.html`, `Landing.dc.html`.

## IMMEDIATE NEXT STEP — scope F1c, then hand the CC launch package
**F1c = the premium right config drawer + the operator's drawer↔modal toggle + the model picker + the run-view.** In scope (all over the EXISTING panel/run-view; backend contract is the WALL — `api.ts` byte-untouched, no migration):
- **The config drawer** — reskin the existing node side panel (`TeamNodePanel` / `SidePanel`) into the design's premium **384px right drawer** (`--shadow-drawer`), consuming F0's tokens. Selecting a node already opens the existing panel (F1b just triggers `setSelectedNodeId`); F1c makes it the premium drawer. The drawer shows the node's identity — the design's subtitles are "Its prompt is its whole identity" (agent/thinker/worker), "A human checkpoint" (gate), "An endpoint of the flow" (terminal).
- **The drawer↔modal toggle** (the operator's tweak) — a header button that flips `panelMode: 'drawer' | 'modal'`; modal = a centered ~560px card with the `tv-modal-in` animation. Design: the toggle icon/title is `#i-dock-right`/"Dock to the side" ⇄ `#i-expand`/"Open as a pop-up".
- **The model picker** — wire the **F1a INERT model footer** (`<button className="rf-node__model">`, currently no handler) to open the model picker. **Reuse Slice-C's existing two-part provider/model picker** already in `TeamNodePanel.tsx` (from Tvashtr-37 — a Provider select from the account's configured providers + an inline "Add a provider" reusing `POST /api/providers` + a provider-scoped Model field + the same-model recommendation hint). Don't rebuild it — move/reskin it into the drawer.
- **The run-view** — the inspector / verdict / EventFeed / PRD views (the run-mode side panel) reskinned to the drawer look, with the run-view inspector subtitle (Working now / Finished / Needs your decision / Not reached yet / etc.).
- **Fold in the §15 gate/terminal post-drop config editing** — F1b kept Ship/Stop separate at add-time and did NOT let you change a terminal's kind or a gate's config after drop; **F1c's drawer is where that lands** (a terminal's ship↔stop; a gate's config). NOTE: the design marks editing gate *copy* as "a planned backend follow-on" — so gate-copy PERSISTENCE is a §15 BACKEND item, out of F1c's FE-only scope (the drawer can show it, not save new gate copy).

**Do this to scope it (same method as F1a/F1b):**
1. Read the live source — the node panel + run-view: `frontend/src/panel/` (find `TeamNodePanel.tsx` / `SidePanel.tsx` + the run-view components: inspector/verdict/EventFeed/PRD) + `frontend/src/panel.css`. Re-check how `App.tsx` wires `selectedNodeId` → the panel, and read the Slice-C picker already inside `TeamNodePanel.tsx`.
2. Read the design's drawer/modal + run-view recipe in `Canvas.dc.html`: the **RIGHT DRAWER / MODAL markup ~lines 317–380**, and the JS ~lines **461** (`panelMode:'drawer'`), **663–664** (`onDrawerClose`/`onPanelMode`), **937–957** (`drawerStyle`/`modalStyle`/`panelToggleIcon`/`panelToggleTitle`/the node-identity subtitles), **1046–1057** (the wiring). The drawer/modal tokens are already in `extend.css`.
3. **Surface F1c's design decisions from the §1 vision, one at a time, each paired with its UX consequence** — likely: how the drawer↔modal toggle behaves (is the choice per-session or sticky?); how the authoring-view vs run-view of the drawer differ; how terminal-kind (ship↔stop) and gate editing appear in the drawer. Decide from the vision, get the operator's nod per decision before the next.
4. Then write `prompts/F1c-*.md` (the detailed brief) + a **lean `/goal`** (4000-char hard limit — see gotcha) and hand the operator the full 3-block launch package.

After F1c: **F2** (dashboard — over `Dashboard.dc.html` + the real `listRuns`/`getTeams`/`listProviders`), **F3** (auth wizard — `AuthWizard.dc.html`; role/use-case collected-NOT-persisted), **F4** (landing — `Landing.dc.html`, from scratch + a scroll-reactive element).

## GOTCHAS specific to right now (read these)
- **⚠️ PARALLEL CHAT SHARES THE WORKING TREE.** A second sequence, **Tvashtr Sidechat-N** (this session it was **Tvashtr Sidechat-4**), runs in this same project and edits the SAME files. **When editing `PROJECTPLAN.md`, use `edit_file` with surgical anchors — NEVER `write_file` (a full overwrite would clobber the side-chat's edits).** `HANDOVER.md` is the main-sequence doc (safe to rewrite). Its double-quotes are STRAIGHT `"` and apostrophes STRAIGHT `'` — match them EXACTLY in `edit_file` anchors (a curly-quote mismatch fails the match; happened this session — always `dryRun:true` first and check the diff).
- **⚠️ PENDING PIVOTS in Sidechat-4.** The operator has **big new pivots/features prepared in the Tvashtr Sidechat-4 chat** that they want folded into `PROJECTPLAN.md` — but they DEFERRED it ("will do later"). **Do NOT chase them; wait for the operator to paste them,** then fold into §14/§15/§16/§17 as appropriate. You cannot read another chat's content — ask the operator to paste the pivots.
- **⚠️ DOCS state.** `PROJECTPLAN.md` + all prompt briefs (F0/F1a/F1b) are **committed** (`b33bb23`). Only `HANDOVER.md` (this rewrite) is pending — the operator commits it now (command handed over). If `git status` shows any other stray untracked `prompts/*.md`, they're harmless and can be added to that commit.
- **⚠️ `/goal` HARD LIMIT: 4000 characters.** CC rejects a longer `/goal` ("Goal condition is limited to 4000 characters"). Put the detail in `prompts/<name>.md`, keep the `/goal` lean + pointing at it. Verify with `wc -c` before handing over (F0 was 3054, F1a 3884, F1b 3922).
- **⚠️ `create_file` writes to CLAUDE'S container, NOT the operator's Mac.** To write a real project file (a `prompts/*.md` brief, etc.), use **`Filesystem:write_file`** (operator's disk). This session a brief was accidentally written via `create_file` to the container and CC couldn't find it on disk — re-wrote it with `Filesystem:write_file`, then verified with `get_file_info` + a head read.
- **The F1b disk audit found it clean:** `api.ts` byte-identical vs a pre-image; git FF `b3e6873`→`937d27d` (+1, unpushed); the F1a `.rf-node*` recipes intact (`canvas.css` diff = 32 removed, ALL chrome/palette, 296 added); both new tests mutation-real (the App test proves the FORWARD EDGE, not just the node). Minor open (carried from F1a): `entryNodeIds` has NO unit test of its own (nice-to-have; fold into a cleanup or F1c).
- **F1b session artifacts (dev-DB only, ZERO repo effect):** CC left the dev servers running, and the live sign-off added 1 worker node / deleted 1 reject edge from the seeded "My team" — the operator can Ctrl-C the servers + reset/re-edit the team.
- **Desktop Code tab is UNUSABLE** (OS-level EPERM crash) → run CC in the **terminal** with `--dangerously-skip-permissions`.

## STANDING OPERATOR DIRECTIVES (carry these forward — inherited every session)
- **The CC `/goal` loop is the build method.** You write ONE lean `/goal` per bounded milestone; CC self-decomposes + runs to green; you audit on disk; operator FF-merges. Scope each `/goal` to a full milestone verifiable in one transcript.
- **The launch package is ALWAYS three copyable blocks inline, verbatim, every time** — (1) shell cmd `cd /Users/adimac/Desktop/Tvashtr && claude --dangerously-skip-permissions`, (2) the full init prompt, (3) the full `/goal`. NEVER "same as before"; NEVER tell the operator to fetch text from a file. (The brief MAY live in `prompts/*.md`, referenced by the `/goal`; the init-prompt + `/goal` text are always reproduced inline.)
- **Every `/goal`'s acceptance makes CC run ALL verification itself** — `make test` / lint / build + all live smoke/e2e targets — and debug to green before `READY_TO_MERGE`. Never hand the operator commands to run. Bug-fix `/goals` are **reproduce-first** (a regression that genuinely fails on the pre-fix code). Visible slices get a **Playwright self-sign-off with screenshots** (targeted `browser_evaluate` + screenshots — the whole-canvas a11y snapshot HANGS on React Flow). End every `/goal` with the §4.7 8-section FINAL REPORT; a recorded `NEEDS_HUMAN` WITH the report is a valid terminal.
- **The disk audit is your main control point.** Never trust CC's self-report: read the changed files, diff "untouched" claims BYTE-for-byte against pre-images (copy files BEFORE a `/goal` runs; stash to `/tmp/` because same-named copies overwrite), reconstruct git state from `.git` plumbing (`.git/refs/heads/<branch>`, `.git/logs/refs/heads/<branch>` — confirm FF-ability + not-pushed; there is NO `git` CLI in the MCP), confirm tests are mutation-real by reading the bodies, and scrutinize (don't rubber-stamp) any deviation.
- **Design decisions: from the vision, one at a time, paired with the UX consequence.** Decide from §1 + the Tvashtr-25 pivot, NEVER from effort-minimization. One design question per exchange, explicit sign-off before the next. Don't present option menus for vision calls — decide (reserve "you choose" for genuine strategic-direction calls). Every decision states what the user SEES/DOES on the canvas/UI.
- **"Extend never replaces"** for the design system: the base token files are frozen; new material is additive (a separate file — `extend.css`).
- **Merge handoff = ALL commands every time** (cd, checkout main, `merge --ff-only <branch>`, verify log, expected tip sha, FF-fail instruction "stop + tell me = divergence", optional cleanup). **Visual sign-off (when a human check is genuinely needed) = a detailed NUMBERED click-by-click script** naming nodes by their VISIBLE label — never abstract.
- **Guardrails survive bypass ONLY as PreToolUse hooks:** the no-push hook (`.claude/hooks/protect-no-push.sh`) + the migration-freeze hook (`protect-migrations.sh`, covers `0001–0018`). A deny rule in `.claude/settings.json` does NOT survive `--dangerously-skip-permissions`.
- **Operator comms:** terse — "proceed"/"go"/"merged"/"done" = ratify + continue; "by the way" = wants a short answer. Procedures one step at a time (give step 1, wait for the report, then step 2). Analogies help. Always hand copyable artifacts. Simple everyday language, minimal jargon.
- **zsh inline comments break commands** — all git/shell blocks must be comment-free (zsh in non-interactive mode runs `#` as a command).
- **PROJECTPLAN.md is large (~800 lines / ~190KB).** Always: `copy_file_user_to_claude` → `grep -nE '^#{1,3} '` for the header index → `sed -n 'START,ENDp'` for targeted sections. Never read it whole. The line-6 header banner is one giant single line — bump it by anchoring on `> **Last updated:** ... — **Tvashtr-N.**` and demoting the old summary to `_Prior:_`. New §17 entries go before the `---` preceding `## 18. Glossary` (anchor on `... [last line of prior entry]\n\n---\n\n## 18. Glossary`).

## Tools / resources
- **Filesystem MCP** — all project reads/writes at `/Users/adimac/Desktop/Tvashtr` (confirm the allowed dir at session start). `bash_tool` runs only in Claude's container (`/mnt/...`), NOT the operator's machine — use it for diffs/greps on files brought over via `copy_file_user_to_claude` (which land at `/mnt/user-data/uploads/<basename>`, are **read-only from bash**, and **silently overwrite same-named copies** — stash pre-images to `/tmp/` first).
- **`edit_file`** — always `dryRun:true` first with unique multi-line anchors; the apply diff is the verifier. Use it for `PROJECTPLAN.md` (surgical; never `write_file`). Use **`Filesystem:write_file`** for `HANDOVER.md` (full rewrite) and for creating `prompts/*.md` briefs (NOT `create_file`, which hits the container).
- **Git state reconstruction** — `.git/refs/heads/<branch>` for the tip sha; `.git/logs/refs/heads/<branch>` for the create→commit chain + parent (FF-ability + not-pushed); `.git/logs/refs/heads/main` shows main's history (docs-closeout commits land DIRECTLY on main, e.g. `b33bb23`). No `packed-refs` — loose refs only.
- **Proven agent model:** `nvidia_nim/meta/llama-3.3-70b-instruct` (`.env TVASHTR_AGENT_MODEL`; ~1.4s/call). `DEFAULT_MODEL` must be a non-reasoning instruct model (e.g. `openai/gpt-4o-mini`) — a reasoning model silently returns empty PRD content.
- **Claude Code** — CLI agent, terminal + `--dangerously-skip-permissions`. `/tvashtr-loop` skill + Playwright MCP available.

## Ready-to-paste first message for Tvashtr-43
> You are Tvashtr-43. Read `HANDOVER.md` and `PROJECTPLAN.md` at the project root first (HANDOVER first); then pick up at scoping **F1c** — the premium right config drawer + the drawer↔modal toggle + the model picker + the run-view. Read the live panel/run-view source (`frontend/src/panel/` + `panel.css`) and the `design/` Canvas drawer/modal recipe, surface the F1c design decisions one at a time, then hand me the full Claude Code launch package. Don't start work until you've read both docs.

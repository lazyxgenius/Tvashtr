# HANDOVER — Tvashtr-46 → Tvashtr-47

> Read this FIRST, then the named `PROJECTPLAN.md` sections, then the relevant source on disk — evidence before design. You are the **architect/planner**; all implementation goes through Claude Code.

---

## 0. First message for the next chat
> You are Tvashtr-47. Read `HANDOVER.md` and `PROJECTPLAN.md` at the project root first (HANDOVER first). Then pick up at **F3 (the auth wizard)** — the next M-frontend slice. Design it (one decision at a time, paired with its UX consequence), write the brief + the launch package, hand it, audit on disk, FF-merge. Don't start work until you've read both.

---

## 1. Where we are RIGHT NOW
- **`main` @ `468d799`.** Alembic head **`0018`** (freeze `0001–0018` holds; NO migration all of Tvashtr-46). Floors on merged `main`: **336 backend** / **191 vitest**. (Note: the vitest floor moved 193 → 191 in F2c — TeamsRail's 5 tests deleted, +3 new Dashboard tests; F4 added +2 landing tests. Always re-baseline from a fresh `make test-frontend` at the start of any FE slice.)
- **The FULL F2 DASHBOARD MILESTONE is SHIPPED + merged** (F2a → F2b → F2-delete → F2c). The dashboard is now the unified teams table with per-team latest-run status + lifetime spend, the New-team template picker, delete-that-stops-runs, a real stat strip, and reskinned providers. **F4 landing hardening also shipped** (responsive/a11y/reduced-motion). See §2.
- **Working tree:** `PROJECTPLAN.md` + `HANDOVER.md` are modified-uncommitted (this closeout) — **they need one commit** (command in §6). Nothing else is dirty (the F2b/F2-delete/F2c branches were FF-merged + deleted; the F4 worktree + branch were removed). The untracked deep-dive `.md` files + `design/` stay untracked, as always. `STATE.md` is the CLI agent's log (tracked, agent-owned — don't touch it).

## 2. What Tvashtr-46 did — ran TWO PARALLEL Claude Code sessions
The operator chose to parallelize (accepting the file-overlap tradeoff). The architect gave a **file-overlap map FIRST** (Session A's backend+dashboard files vs Session B's 3 `.tv-lp` landing files → the ONLY shared file was `STATE.md`) and the operator approved before any `/goal`.
- **Session A — the serialized F2 line** (one branch at a time, each FF-merged onto the prior tip):
  1. **F2b** (`3949e26`, off `34c2f59`) — enriched `GET /api/teams` so every team summary carries `last_run {status,at,run_id}|null` + `spend_usd`, joined READ-ONLY via the existing `cloned_from_node_id` clone→origin link (new batched `_run_rollup_by_origin` in `teams.py`, a `DISTINCT` subquery collapsing the node-fan-out to one row per run before aggregating, `DISTINCT ON` latest + `SUM(COALESCE)` spend, no N+1). Backend 331 → 336. No migration.
  2. **F2-delete** (`825adea`) — rewired `DELETE /api/teams/{id}` to FIRST cancel any in-flight run (shared `cancel_run_core`, EXTRACTED from `cancel_run`) THEN hard-delete the team + ALL its runs (run-scoped rows across all 5 `run_id`/`workflow_id`-keyed tables → `Run` → clone `TeamGraph` → team, FK-safe; cancels OUTSIDE the delete txn). Closes the zombie-run bug. Backend 336 → 340. No migration.
  3. **F2c** (`9c14a46`) — reskinned `Dashboard.tsx` to `design/Tvashtr Frontend Overhaul/Dashboard.dc.html`: the ONE unified teams table, the New-team template picker (new `NewTeamDialog.tsx`), the delete confirm (stops-run warning), the real stat strip, reskinned providers, the `.rf-node__meta` `flex-wrap` RIDER (the §15 status-pill overflow item — RESOLVED), and `TeamsRail` removed. `api.ts` ADDITIVE-only. vitest 193 → 191. No backend/migration.
- **Session B — F4 landing hardening** (`f09a83a`, in its OWN git worktree off `main`, cherry-picked LAST as `468d799`) — a `<main>` landmark + skip link + named nav, mobile Sign-in kept reachable, a complete `prefers-reduced-motion` reset, contrast lifted to AA, the wide how-it-works board folded on phones, a branded focus ring. Confined to `LandingPage.tsx` + `landing.css` + `LandingPage.test.tsx`.
- **All four DISK-AUDITED** (git plumbing from `.git`; a byte-diff of `teams.py` vs a stashed F2b pre-image proving F2-delete is additive-only; a `models.py` grep confirming the 5-table run-scoped census is complete → no orphan; the `runStatusPill`-in-`status.ts` deviation read + judged sound; mutation-real + genuine reproduce-first test bodies read; `git diff --stat`/`git show --stat` for every scope). Full as-built in **PROJECTPLAN §17 (2026-07-04)**.

## 3. IMMEDIATE NEXT STEP — F3 (the auth wizard)
The M-frontend ordering: F0✅ → F1a✅ → F1b✅ → F1c✅ → (canvas fidelity 1–2✅) → F4 landing✅ (+ hardening✅) → **F2 dashboard✅** → **F3 (auth wizard) ← NEXT** → any F4-polish. **M-frontend is nearly complete** — F3 is the last major slice.

**F3 = the auth wizard** (per the M-frontend decision, PROJECTPLAN §16): after login/register, a short wizard that collects the user's **role + use-case** — **collected, NOT persisted** (no new column, no migration). It hands off into the dashboard. Design it from the vision + the `design/` target (look for an auth/onboarding mockup in `design/Tvashtr Frontend Overhaul/`). Frontend-only, no migration, executor byte-intact.
- **You must DESIGN F3 first** (it isn't briefed yet — unlike F2's slices, which were pre-briefed). One decision at a time, each paired with its concrete UX consequence, explicit operator sign-off before the next. Then write `prompts/F3-auth-wizard.md` + the launch package.
- **Overlap note:** F3 touches the auth flow (`AuthGate.tsx`, `App.tsx`, `LoginScreen.tsx`, + a new wizard component). F2c already merged, so there's no live parallel session — F3 is a normal single-session FF slice. If the operator wants to parallelize again, the same file-overlap-map-first discipline applies.
- **`/goal` LENGTH CAP: 4000 characters.** (Learned this session — a 5446-char F2c `/goal` was rejected.) Keep every `/goal` under 4000 chars; lean on the `prompts/*.md` brief for the granular detail (the `/goal` says "Follow prompts/<x>.md exactly" and points CC at it). Verify the char count before handing it (`wc -c`).

## 4. Standing operator directives (CARRY FORWARD — inherited every session)
- **Role = architect/planner ONLY.** All implementation (product code, diagnostic/throwaway scripts, build/Makefile/config) goes through a Claude Code `/goal`. Architect-direct edits are ONLY: the two living docs, `prompts/*.md` briefs, and trivial doc/typo fixes. When tempted to write code, write a `/goal`.
- **The disk audit is the main control point.** Never trust the self-report — read the changed files, byte-diff "untouched"/"additive-only" claims against a pre-image stashed to `/tmp/`, read test bodies to confirm they're mutation-real, and for a bug-fix confirm the reproduce-first regression genuinely goes RED on the old code. **Scrutinize EVERY deviation from the brief** — read the actual change and judge it yourself (this session the `runStatusPill`-in-`status.ts` deviation was sound; verifying it required reading `StatusPill.tsx` to confirm it was canvas-status-locked). The **`git diff --stat main <branch>` / `git show --stat <sha>`** is the free scope proof (→ byte-intact set by omission).
- **No `git` CLI in the MCP** — reconstruct git state from `.git` plumbing: `.git/refs/heads/<branch>` (tip; a `feat/x` branch's ref is at `.git/refs/heads/feat/x`), `.git/logs/refs/heads/<branch>` (reflog → parent chain + FF-ability + nothing-pushed). A worktree's branch ref lives in the MAIN repo's shared `.git/refs/heads/` (readable). **Operator is your git-CLI proxy** for a diff you can't compute — give ONE clean command (use `git diff --stat main <branch> | cat` to avoid the pager; plain `git show --stat` opens `less`, tell them to press `q`).
- **The copy-cache gotcha:** `copy_file_user_to_claude` caches by basename — a 2nd copy of a file you already copied returns the STALE version. Before re-copying a changed file, `mv` the old container copy to `/tmp/` (that also gives you a pre-image to diff against). `Filesystem:read_text_file` is cache-free (but has only `head`/`tail`, no ranges — use the container copy + `sed` for mid-file ranges).
- **Editing the operator's disk:** `Filesystem:edit_file` (line-based, `dryRun` optional but the returned diff is the verifier — use exact multi-line anchors) or `Filesystem:write_file` (full overwrite — used for the HANDOVER rewrite). `create_file`/`str_replace`/`bash_tool` write to CLAUDE's container ONLY (invisible to the operator).
- **Merges: operator FF-merges** (`git merge --ff-only`). ALWAYS give ALL merge commands as copyable text (cd, checkout, merge, verify line + expected sha, what-if-it-fails, branch cleanup). A parallel/worktree branch that DIVERGED merges by **cherry-pick** (`git cherry-pick <sha>`; then `git branch -D` FORCE, because a cherry-picked branch isn't a literal ancestor; + `git worktree remove <path>`).
- **Every Claude Code handoff = the COMPLETE copyable launch package, every time** (even if unchanged): (1) `cd /Users/adimac/Desktop/Tvashtr && claude --dangerously-skip-permissions`, (2) the FULL init prompt verbatim, (3) the FULL `/goal` verbatim (**under 4000 chars**). NEVER "same as before"; NEVER point them at a `prompts/` file for the init/`/goal` text. The detailed BRIEF lives in `prompts/<n>.md` (the `/goal` references it).
- **`/goal` = one bounded milestone**: outcome + invariants-as-on-disk-evidence + the acceptance checklist CC runs ITSELF to green (make test/lint, FE build+vitest, live smoke, Playwright self-sign-off with a screenshot per check) + reproduce-first for bug-fixes + stop conditions (NEEDS_HUMAN + turn cap; distinguish "unknown/broad → stop" from "code-proven contained fix → proceed"). CC runs ALL verification — never hand the operator manual commands.
- **Design decisions: from the vision (§1) + the Tvashtr-25 pivot, never from effort. One at a time, explicit sign-off before the next. No option menus for vision calls — decide. Pair EVERY decision with its concrete user-facing UX consequence.**
- **Deferred items → §15 register IMMEDIATELY** (with the decision that deferred them).
- **Visual sign-off = a concrete, NUMBERED, click-by-click script** (name the exact team by rail label + the exact node by VISIBLE label + the exact action + explicit pass/fail). Default to CC's Playwright self-sign-off with screenshots; the operator eyeball is the backstop.
- **Bypass mode caveat** (`--dangerously-skip-permissions`): allow/deny rules go INERT — only PreToolUse hooks survive. `protect-migrations.sh` (freeze) + `protect-no-push.sh` (no-push) are BOTH hooks, so they hold. The agent NEVER pushes; commits only its OWN changed paths (leaves the living docs + `prompts/*.md` for the architect).
- **Communication (operator preferences):** terse — "proceed"/"go"/"merged"/"done" = ratify + move on; "By the way" wants a short answer. **Procedures ONE step at a time.** **Analogies help. Simple, everyday language — avoid jargon.** Always hand copyable artifacts; never make them scroll back or reconstruct.
- **Session hygiene:** watch context; wrap up cleanly BEFORE running out (warn proactively). "handover" from the operator is an immediate trigger. Close out = PROJECTPLAN (§17 + §15 + header) then a full HANDOVER rewrite then the ready-to-paste opener.

## 5. Parallel-session playbook (proven this session)
If the operator wants two Claude Code sessions again:
- **Give the file-overlap map FIRST and wait for approval** — name the exact files each session touches and where they collide, before any `/goal`.
- **The ideal Session-B candidate is a FE-only, DATA-LESS, disjoint-file slice** (the landing was perfect). It needs NO backend/DB, runs in its OWN `git worktree` off `main` on a DIFFERENT vite port (5174, so it doesn't clash with the main session's 5173), and its files can't collide.
- **Honor the three hard invariants INSIDE the session** (a merge can't fix them): (1) NO new migration — two branches each adding one produces two heads + a broken chain git won't flag; (2) executor (`team_run.py`/`graph_validity.py`) byte-intact; (3) minimize file overlap.
- **The overlapping session merges LAST** — it diverged, so a plain `git merge`/`cherry-pick`, resolve `STATE.md` by keeping main's copy (F4 didn't even commit `STATE.md`, so it was a truly clean cherry-pick — a session may or may not touch it).
- **The worktree is OUTSIDE the architect's allowed filesystem dirs** — you can't read its files directly. Verify its scope via the operator-proxy `git show --stat <sha>` (proves the file-set), and do the content glance AFTER it lands in the main repo (post-merge/cherry-pick).

## 6. Gotchas & tools
- **Commit the docs closeout** (the only dirty state):
  ```
  cd /Users/adimac/Desktop/Tvashtr
  git add PROJECTPLAN.md HANDOVER.md
  git commit -m "docs(tvashtr-46): F2 dashboard milestone complete (F2b+F2-delete+F2c) + F4 landing hardening; handover to Tvashtr-47"
  ```
  (The deep-dive `.md` files + `design/` stay untracked. No `prompts/*.md` were authored this session — F2's briefs were already committed by Tvashtr-45; F3's brief is Tvashtr-47's to write.)
- **PROJECTPLAN.md is ~250KB / ~880 lines.** NEVER read it whole. Copy it (`copy_file_user_to_claude`) → `grep -nE '^#{1,3} '` for a header index → `sed -n 'A,Bp'` targeted reads. Key sections: §1 vision, §6.4 the human-authored-graph constitution, §14 features, **§15 roadmap + deferred register**, §16 milestones, **§17 append-only as-built log**, §18 glossary.
- **The seeded dev team is "My team"** = the `review_loop` template: visible node labels **Product manager** (entry), **Engineer**, **Reviewer** (workers), **PRD approval** + **Escalation** (gates), **Ship** + **Stop** (terminals). Bring-up: `make db-up && make migrate && make seed && make backend` + `make frontend` (Vite :5173); login `operator@tvashtr.local` / `tvashtr-dev`. (F2c left dev servers up + disposable demo teams in the dev DB — a `make seed`/DB reset clears them.)
- **Backend key facts (verified this session):** `Run.id == Run.workflow_id == the DBOS workflow id` (set at creation: `Run(id=run_id, workflow_id=run_id)` + `SetWorkflowID(run_id)`) — so any `run_id`-keyed lookup and `DBOS.cancel_workflow(str(run.id))` are consistent. The run→library-team link is per-NODE (`AgentNode.cloned_from_node_id`), NOT a direct FK; `_run_rollup_by_origin` (F2b, in `teams.py`) + `_latest_invocation_by_origin` (in `routers.py`) both do this clone→origin join. The SHARED cancel core is `cancel_run_core` in `teams.py` (`DBOS.cancel_workflow` + `Run.status='cancelled'` + close pending `HumanTask`s); `cancel_run` (routers.py) + `delete_library_team_and_runs` (teams.py) both call it. Run-scoped tables (`cost_records` keyed by `workflow_id`; `run_events`/`agent_invocations`/`human_tasks`/`engineer_run_attempts` keyed by `run_id`) have NO FK cascade → an ordered multi-table delete.
- **FE status is centralized in `frontend/src/lib/status.ts`** — the single source of truth for all derived status (`deriveNodeStatus`, `deriveGateState`, `deriveOverall`, and now `runStatusPill` for the dashboard). Extend it there, NOT inline in components. `StatusPill.tsx` is typed to the canvas `NodeStatus` vocab with fixed labels — it can't render dashboard run-status labels (that's why F2c added `runStatusPill`).
- **Tools:** Filesystem MCP (operator disk — `read_text_file`/`read_multiple_files`/`edit_file`/`write_file`/`list_directory`/`copy_file_user_to_claude`; load them via `tool_search` at session start), `bash_tool` (Claude's container only — for files copied into `/mnt/user-data/uploads/`), Playwright MCP (automated UI/E2E inside a `/goal`). Briefs: `prompts/CLI-RULES.md` + `prompts/CLI-SETUP.md` (operating contracts).
- **Deferred behind the frontend wall** (do NOT start until M-frontend is done): the Sidechat-4 deep-dive milestones **M-ctx0 → M-ctx1 → M-ledger → M-tools → M-rails** (migrations `0019–0021`, freeze-bumped last), and the **brownfield stronger-worker thesis re-run** (DEFERRED, NOT queued). **Mv (a non-founder shipping real value on their own repo) remains the real demand gate.**

## 7. Open questions / to-confirm
- **F3 design is not yet done** — it's Tvashtr-47's first real task (design → brief → build). Check `design/Tvashtr Frontend Overhaul/` for an auth/onboarding mockup; read `AuthGate.tsx`/`App.tsx`/`LoginScreen.tsx` to know the seam. Role/use-case is collected-NOT-persisted (no migration).
- **§15 deferrals added this session** (F2 reviews): durable run→library-team FK (needs a migration); orphaned PM `documents` on run teardown (retention/product call); app-wide modal a11y (focus-trap + Escape on `.tv-scrim` pop-ups); `NewTeamDialog` unmounted-setState guard; per-run history drill-down from a team row. All non-blocking; the operator hasn't scheduled any yet.
- **After F3 + any F4-polish, M-frontend is DONE** — at which point the deferred wall lifts and the operator decides the next track (the brownfield thesis re-run, or the M-ctx0…M-rails deep-dive milestones, or the Mv demand probe).

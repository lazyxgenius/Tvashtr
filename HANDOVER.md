# Tvashtr — HANDOVER (for Tvashtr-29)

> Read this **and** `PROJECTPLAN.md` (§1 vision, §15 deferred register, §16 milestones, §17 decision log — esp. the Tvashtr-25/26/27/28 entries) before taking any action. This file is the session-continuity snapshot; PROJECTPLAN is the source of truth.

---

## 1. Where we are right now

- **`main` @ `df4957a`** (the P1.8c **generic "thinker" node + capability authoring** slice — Tvashtr-28 — is FF-merged). Alembic head **`0013`** (NO migration this slice). **209** offline backend tests, **95** vitest, ruff clean, `build-frontend` clean. The live `make thinker-chain-e2e` + `make capability-edit-e2e` are GREEN (real NIM), and the four regression smokes (skeleton-run/crash, loop-run/crash) are GREEN.
- **Uncommitted in the working tree:** `PROJECTPLAN.md` + `HANDOVER.md` (this file) — the architect's Tvashtr-28 docs closeout. **The operator commits these as a separate docs-closeout commit on top of `main`** (the agent never sweeps the architect's doc edits). After that commit, the tree is clean.
- **What just shipped (Tvashtr-28):** the pivot's **last fixed-function residue** is retired. Before this slice a `completion` node ("thinker") was valid ONLY as the start node, and the start thinker was the sole writer of the shared spec every worker reads — so a thinker was not composable anywhere else, and no valid capability flip existed on the templates. Now:
  - A thinker is composable **anywhere**. `run_graph`'s completion branch dispatches first-vs-later on `pm_document_id is None`: the **first** thinker still runs `pm_step` (kept **byte-identical** — same `pm-llm` cost key + `pm-prd-v1` doc key, so the three smoke checkers + every offline executor stub stayed untouched); a **later** thinker runs the new additive `thinker_refine_step`, which reads the current spec via the recorded `read_latest_prd_step` and appends a refined version (per-node cost/doc keys, idempotent → crash-resume re-reads the same recorded spec and re-appends once). The old non-start-completion `else`-fail is removed. `agent_run_step`/`read_latest_prd_step`/the engines are untouched.
  - **Capability (thinker↔worker) is authorable** on the node-edit panel. The `PATCH /api/teams/{id}/nodes/{id}` body gained an optional `capability` ("thinker"/"worker") that maps server-side to a paired `(kind, engine)` write (`thinker`→`(completion, None)`, `worker`→`(agent, "openhands")`). The ONE executor invariant — the root writes the shared spec, so it must stay a thinker — is a backend **409 if `worker` is asked of the root**. The FE adds a segmented Capability control (seeded from `kind`, disabled+tooltip when the node is the start node) + a canvas re-label on flip. A new `thinker_chain` template (PM → Architect → Engineer) exercises a real non-start thinker.

## 2. The operating contract (unchanged — internalize before acting)

- **You are the ARCHITECT/PLANNER only.** You design milestones, write the `/goal` brief (saved as `prompts/<name>.md`), and **audit Claude Code's work by reading files on disk**. ALL implementation — product code, throwaway/diagnostic scripts, build/Makefile/config — goes through Claude Code. Your only direct edits: the two living docs, `prompts/*.md`, trivial typo/comment fixes.
- **TWO STANDING OPERATOR DIRECTIVES (permanent — in memory, do not drift):**
  1. Make ALL design/scoping decisions **directly from (a) the Tvashtr-25 pivot and (b) the §1 vision** — never path-of-least-resistance or effort-minimization.
  2. Whenever you present a **decision** to the operator, **also describe the concrete FE/UX consequence** — what the user sees and does on the canvas/rail/panel — every time, not just the architectural change.
- **The disk audit is the real control point.** Never trust Claude Code's self-report. Read the changed files; reconstruct git state from `.git` plumbing (you have no git tool — see §6); verify the byte-intact invariants; confirm the new tests are mutation-real (non-vacuous — read the test bodies, not the count). **You read the pre-run `main` versions of the files you'll need to diff against BEFORE launching Claude Code** — that's how you check "byte-intact" without git.
- **Execution loop:** you write ONE lean-but-detailed `/goal` brief per bounded milestone → operator launches Claude Code under `--dangerously-skip-permissions` (bypass) → it runs to evidence on a branch → you audit on disk → operator FF-merges → you do the docs closeout. **The FF diff-stat (`git diff --stat main <branch>`) is the authoritative file-set gate** for "X untouched" invariants — ask the operator to run it (and `git diff main -- <file>` for a specific empty-diff claim like `team_run.py`'s `agent_run_step`).
- **Operator comms:** terse ("proceed"/"Go"/"merged" = proceed). "By the way" = wants a short answer. Procedures one step at a time. Analogies help. Give exact copy-paste commands when asked (comment-free — zsh doesn't strip inline `#` in non-interactive mode).

## 3. What's done — the prompt-driven rebuild (P1.8) so far

The Tvashtr-25 **pivot** retired fixed-function role nodes for a fully **prompt-driven node model**: a node = `{prompt (its whole identity) + capability (thinker=completion / worker=agent, on the existing kind column) + model}`. The executor stops branching on `role_name`/`config.agent_kind`; routing is the **authored graph topology** (a generic outcome-label any node emits, matched by `Edge.conditions {when:<label>}` via the pure `next_node`; the loop-back is a no-`when` catch-all carrying `{loop_limit:N}`). Gates + terminals stay deterministic control primitives. The `teams.py` builders became an editable **template library**.

- **P1.8a (Tvashtr-25, `dc5a984`):** the backend executor core. Migration `0012` (`agent_nodes.prompt`); `run_graph` de-roled to one generic `agent_run_step`; `node_emits_outcome` replaces `agent_kind`; the reviewer loop-back retopologized to the `{loop_limit:N}` catch-all.
- **P1.8b-1 (Tvashtr-26, `b7287fb`):** the first FE authoring slice — ONE persistent editable team on a read-only canvas, click-a-node → prompt/model panel, clone-on-launch. (Eyeball caught a latent P1.8a regression — the dashed loop-back arc keyed on the now-dead `when=="changes_requested"`; fixed to key on `loop_limit != null`.)
- **P1.8b team library (Tvashtr-27, `4443968`):** multiple persistent teams (`team_graphs.is_library`, migration `0013`) + the left teams rail + the drop-and-edit template library.
- **P1.8c (Tvashtr-28, `df4957a`):** THIS slice — the generic thinker (composable anywhere) + capability (thinker↔worker) authoring + the `thinker_chain` template (detail in §1 + the §17 Tvashtr-28 entry). NO migration.

**With P1.8c, the "a node = {prompt, capability, model}" authoring triple the pivot defines is fully editable** — prompt (P1.8b-1), model (P1.8b-1, the free-text slug + `MODEL_PRESETS`), and now capability (P1.8c). What's NOT yet editable: the **topology** (the canvas is still `nodesConnectable={false}`) — that's the M2 marquee.

## 4. The next slice — a genuine strategic-direction call (present both, recommend)

Capability editing is **done**. Two candidates for next; this is a real value-ordering call, so tee it up for the operator with the FE/UX consequence of each (don't silently pick):

**Option A — the per-node work-brief (the backward half of the two-layer document model; smaller, my recommendation).**
Generalize `AgentInvocation.outcome_detail` (today reviewer-only — it holds the Reviewer's verdict reasons) to **every node**, so each node persists a short "what I did last run" brief, and **surface it on the canvas** (a per-node panel reads it). This is the backward-facing presentation layer (the shared spec is the forward-facing one). It's small (additive — generalize an existing nullable column's writer + a read endpoint + a FE panel; likely NO migration since the column already exists), and it's the **substrate for "converse with a node" Mode A** (explain-what-you-did over recorded history). **FE/UX:** click a node → see a concise "last run" summary of what that node contributed (e.g. the Engineer's "added `overdue_check` + tests"; the Reviewer's verdict reasons it already shows). Gives the human a per-node read of the run without scrolling the raw event feed.

**Option B — canvas topology editing (`nodesConnectable` ON — the J2 "author your own wiring" marquee; bigger).**
Turn off `nodesConnectable={false}` and let the user draw/delete edges and add/remove nodes — the *core* composability differentiator ("the team is mine"). This is a meatier slice: node/edge CRUD endpoints + the React Flow connect/delete handlers + **holistic graph-validity** (exactly one root that is a thinker, every worker reachable only after a spec exists, no orphan/dead-end non-terminal, the loop-back/escalation shape well-formed — see the §15 "Holistic graph-validity" item; M1 enforces ONLY root-is-a-thinker via the PATCH 409). **FE/UX:** the canvas becomes fully editable — drag to connect nodes, delete edges, drop new nodes from the template/blank palette; an invalid graph is flagged before it can run. This is the headline differentiator but the larger build.

**Then (after whichever):** **M3 — mid-run prompt re-read** (reuse P1.7's `read_latest_prd_step` read-latest-before-each-use pattern, applied to the node's `prompt`, so a mid-run prompt edit reaches the agent on its next node entry) → **"converse with a node"** (the §15 named feature: Mode A explain over recorded history; Mode B command→the node rewrites its OWN prompt, propose-then-approve, riding the M3 mechanism). Both M3 and "converse" depend on the per-node brief (Mode A's substrate) + the M3 mechanism, so **Option A is also the natural unblocker for the converse feature** — another reason to lead with it.

**My recommendation:** **Option A (the per-node work-brief)** — smallest, completes the two-layer document model's backward half, and unblocks "converse with a node." Option B (topology) is the marquee differentiator and the right *next-next*, but it's a bigger slice; sequence it after A unless the operator wants to push the differentiator forward. (Per directive #1, ground the final call in §1 + the pivot, not in which is less work — surface that the recommendation is value-ordered, not effort-minimized.)

## 5. Standing strategic gate (unchanged — the real prerequisite for M2)

**The Wizard-of-Oz demand probe** — at least one **non-founder** user validating willingness to pay (Mv) — remains the standing value gate that **no amount of shipping resolves**. M1 (the machinery) is proven; M2 (value/composability earns its keep) is gated on demand validation, not more features. Surface this when the operator weighs "what next" at a milestone boundary. Brownfield development is the registered demand-aligned north star shaping post-capstone sequencing.

## 6. Gotchas & mechanics (not all in PROJECTPLAN)

- **No git tool — reconstruct state from `.git` plumbing via the Filesystem MCP:** `.git/refs/heads/<branch>` for a SHA (nested paths like `feat/x/y` → `.git/refs/heads/feat/x/y`); `.git/logs/HEAD` (tail via `copy_file_user_to_claude` → bash) for the reflog + FF-ability (the branch should show `<main-sha> -> <main-sha> | checkout: moving from main to <branch>` then `<main-sha> -> <tip> | commit:`); `backend/alembic/versions/` listing to confirm the migration set (this slice ends at `0013` — confirm there's no `0014`). **No `packed-refs` in this repo** (loose refs). The authoritative "which files changed" + "X untouched" gate is the operator's `git diff --stat main <branch>` (you can't run it).
- **Byte-intact verification:** read the pre-run `main` version of each do-not-touch file BEFORE the agent runs (into your context), then read the branch version after and compare. For `team_run.py` the load-bearing invariants this slice were **`pm_step` byte-identical** (the first-thinker path — its `pm-llm`/`pm-prd-v1` keys must not move) and **`agent_run_step` untouched** (the worker path); the operator's `git diff main -- team_run.py` is the proof the no-git audit can't fully run. When a future slice claims "executor untouched," lean on that empty-diff.
- **Proven agent model:** `nvidia_nim/meta/llama-3.3-70b-instruct` (`.env TVASHTR_AGENT_MODEL`; ~1.4s/call). PM/`DEFAULT_MODEL`: `openai/gpt-4o-mini` (a *reasoning* model as the PM emits an empty PRD — silent failure; the gateway passes empty-as-success). `qwen3-next-80b-a3b` is PARKED (~40% `TextContent is not JSON serializable`). NIM occasionally emits empty output on a forced-revision round — a known real-agent flake, not a regression.
- **FE testing:** `user-event` ⊥ vitest fake timers → use `fireEvent` when the test drives virtual time. React Flow needs the jsdom shims in `frontend/src/test/setup.ts` (`ResizeObserver`/`DOMMatrixReadOnly`/box-metrics/`getBBox`) or the canvas renders zero nodes under RTL. The **tsc-strict `build-frontend` gate does real work** — a signature mismatch (e.g. a call against the now-extended `updateTeamNode` that gained the optional `capability`) fails the build, which is how cross-file FE wiring is implicitly verified.
- **Bypass mode** (`--dangerously-skip-permissions`): the permission/deny layer is inert; only the two PreToolUse hooks survive — `protect-migrations.sh` (freeze, now `^00(0[1-9]|1[0-3])_` = `0001`–`0013`) and `protect-no-push.sh` (no `git push`). When the next slice creates migration `0014`, the brief must bump the freeze regex to include it (as its LAST step — once frozen, the hook blocks further edits to it). Editing the hook *file* is allowed (it guards migration files, not itself). **Option A (the work-brief) likely needs NO migration** (`outcome_detail` already exists); **Option B (topology) probably DOES** add a migration (edges/nodes CRUD may need schema), so plan the freeze bump if B is chosen.
- **DBOS PENDING recovery:** leftover PENDING runs from prior sessions resurrect on backend restart → dev-DB reset (`docker compose down -v` → `make db-up` → `make migrate`) clears them. **But:** a reset wipes the real `"My team"` row, so `seed_library_if_empty` creates a *fresh* one instead of the `0013` promote — don't reset before verifying the `0013` promote if that evidence matters.
- **`PROJECTPLAN.md` is ~391KB; line 6 is a ~290KB single-line "Last updated" banner.** Edit surgically: `copy_file_user_to_claude` → bash `grep -nE '^#{1,3} '` for the header index (current: §1 line 22, §6 line 141, §9 line 323, §15 line 476, §16 line 575, §17 line 585, §18 line 815 — these shift as entries are appended) + `sed -n 'X,Yp'` for sections; `edit_file` with `dryRun:true` first, anchored on **unique multi-line strings** (the standing closing line "...remains the real value gate." repeats in every §17 entry, so anchor the §17 append on the previous entry's unique NEXT-line + the trailing `\n\n---\n\n## 18. Glossary`). §15 deferred items append before the `---` preceding `## 16. Milestones`. `write_file` for the full `HANDOVER.md` rewrite (the param is `content`, not `file_text`). `edit_file` takes an `edits` array — batch all surgical edits into one call (one dryRun + one apply); a single bad anchor fails the whole batch atomically, so dryRun-verify first.
- **CLI-RULES.md / CLI-SETUP.md are STALE** (predate the Tvashtr-25 pivot): they say the freeze is `0001`–`0011`/`0009`, reference a non-existent `graph_runner.py`, name the milestone "Supervisor-first onboarding," and carry old test counts. The init prompt (in the opener below) neutralizes this by pointing the agent at the brief + HANDOVER as authoritative. **Refreshing these two docs is a worthwhile small architect-direct cleanup** (the migration-freeze number → `0013`, the milestone → the prompt-driven rebuild, the `graph_runner.py`→`backend/tvashtr/control_plane/team_run.py` reference, the counts → 209/95) — do it opportunistically; not blocking.

## 7. Living docs & how to maintain them

- **`PROJECTPLAN.md`** — source of truth (§1 vision, §6 arch, §7 decisions, §8 stack, §9 data model, §13 risks/S1–S3, §14 feature breakdown, §15 roadmap + the deferred register, §16 milestones, §17 append-only decision log, §18 glossary). Update at the end of each step: the §17 as-built entry (append immediately before the `---` preceding `## 18. Glossary`), §15 deferral additions/reconciliations, and the line-6 header "Last updated" bump. **New this session in §15/§17:** the two-layer document model (named upgrade), "converse with a node" (named feature), per-node document routing, holistic graph-validity, the capability-flip limitation, the stale-`pm_step`-docstring + renames — all registered; the §9.2 Document entity carries the two-layer note; the Tvashtr-22 memory entry carries the three-orthogonal-layers refinement.
- **`HANDOVER.md`** — this file; fully rewritten each session close.
- **`STATE.md`** — Claude Code's running execution log (the agent owns it; you read it, don't maintain it).
- **`prompts/`** — the `/goal` briefs (e.g. `p1.8c-thinker-capability.md` — the just-shipped one), `CLI-RULES.md`, `CLI-SETUP.md`. Architect-direct.

---

## Ready-to-paste opener for Tvashtr-29

> You are Tvashtr-29. Read `HANDOVER.md` and `PROJECTPLAN.md` (§1, §15, §16, §17 — especially the Tvashtr-25/26/27/28 entries) at the project root first. Then pick up at the **next-slice decision** (HANDOVER §4): present the operator **two options with their FE/UX consequences** — **(A) the per-node work-brief** (generalize `AgentInvocation.outcome_detail` to every node + surface a "what I did last run" panel on the canvas; smaller, likely no migration, unblocks "converse with a node"; the recommended next) vs **(B) canvas topology editing** (`nodesConnectable` ON + node/edge CRUD + holistic graph-validity; the J2 "author your own wiring" marquee, bigger) — recommend A, grounded in the §1 vision + the Tvashtr-25 pivot (value-ordered, not effort-minimized), and let the operator choose before you scope the `/goal`. Don't start work until you've read both docs. First confirm the project root is the allowed directory, and note the docs-closeout state: `main` should be at `df4957a` (the P1.8c slice) with the Tvashtr-28 doc edits committed on top.

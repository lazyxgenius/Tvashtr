# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ---- Node / frontend ----
node_modules/
frontend/dist/
*.local

# ---- Env / secrets ----
.env
.env.*
!.env.example

# ---- Editor / OS ----
.DS_Store
.idea/
.vscode/
*.swp

# ---- Agent workspaces (P0.3, local-unsandboxed) ----
.tvashtr_workspaces/
# ---- Hosted-GitHub per-run clones (M-h1b; deterministic per-run dir, throwaway) ----
.tvashtr_clones/

# ---- Logs / tmp ----
*.log

# ---- Playwright MCP (browser-gate artifacts: snapshots, screenshots, console logs) ----
.playwright-mcp/

# ---- tvashtr-loop scratch (ephemeral resume state; durable record is §17 + the loop report) ----
.tvashtr/

# ---- CLI-agent running log (per-session scratch, overwritten each run; durable record is §17) ----
STATE.md

# stray root-level screenshots (visual-acceptance artifacts)
/*.png

# Playwright run output (traces/screenshots on failure) from the e2e gates.
frontend/test-results/
frontend/playwright-report/

# ---- M-proof (make demo-proof / demo-proof-prod / demo-proof-login) ----
# Per-check browser screenshots for the C1-C12 harness; regenerated on every run.
artifacts/
# The saved PROD Playwright storageState. This is a LIVE SESSION CREDENTIAL for the deployed app:
# never print it, never commit it. Recreate with `make demo-proof-login`.
.playwright/
.superpowers/

# /goal — P1.8b-1 eyeball fix: the missing dashed loop-back arc (FE-only, EXISTING feat/p1.8b-editable-team branch)

> One visual issue from the operator's eyeball of P1.8b-1 (`feat/p1.8b-editable-team` @ `48a440e`).
> FE-only. **Stack the fix on the existing branch (on `48a440e`) — do NOT open a new branch; the
> slice is un-merged.** RUN the FE gates yourself and debug to green before `READY_TO_MERGE`. The
> model-field preset UX is intentionally LEFT AS-IS (the operator is satisfied with the current
> datalist) — do NOT touch `TeamNodePanel.tsx`.

## Fix — the Reviewer→Engineer dashed loop-back arc is missing (a latent P1.8a regression)
**Root cause (verified on disk):** `frontend/src/canvas/TeamCanvas.tsx`'s edge builder renders the
calm dashed `ReworkEdge` ONLY for `e.conditions?.when === "changes_requested"`. P1.8a retopologized
the reviewer loop-back to the no-`when` catch-all `{ "loop_limit": N }` (no `"when"`), so that
condition now matches NO edge — the loop-back falls through to the plain forward styling and the
cyclic review loop is no longer drawn as the distinctive arc. The edge still exists (the run still
loops); only its rendering regressed.

**Fix:** identify the loop-back by its current, unique signature — the edge carrying
`conditions.loop_limit` (the no-`when` catch-all loop-back; `loop_limit_for` already keys on this
key). Render the SAME dashed Bottom→Bottom `ReworkEdge` for it (keep the bottom-handle routing, the
arrowhead, and the "changes requested" label exactly as before). The stale
`when === "changes_requested"` branch no longer matches any seeded edge — replace its condition with
the `loop_limit` check. *(When user-drawn topology editing lands in a later slice, loop-back
detection generalizes; for the seeded review-loop team, `loop_limit` is the correct unique signal.)*

**Update `frontend/src/canvas/TeamCanvas.test.tsx`:** the fixture's loop-back edge must carry
`{ loop_limit: ... }` (matching the real seeded topology, NOT the stale `{ when: "changes_requested" }`),
and the test must still assert EXACTLY ONE `.rf-edge--rework` (the `loop_limit` loop-back) — proving
the loop-back renders dashed under its real signature, and that a reject/escalation edge does not.

## Invariants / do-not-touch (express as checkable evidence)
- **FE-only** — `git diff main --stat` shows nothing under `backend/`. No migration (head `0012`).
  `backend/tvashtr/control_plane/team_run.py` untouched.
- **`TeamNodePanel.tsx` untouched** — the model-field UX stays as-is (operator's call).
- The canvas stays `nodesConnectable={false}` (no topology editing).
- Honor the two FE gotchas: `fireEvent` (NOT `@testing-library/user-event`) when a test drives
  virtual time with fake timers; rely on `frontend/src/test/setup.ts`'s React Flow jsdom shims.

## Acceptance (Claude Code RUNS to green before `READY_TO_MERGE`; echo each)
- `make test-frontend` green, with the updated `TeamCanvas.test.tsx` genuinely asserting the
  `loop_limit` edge → EXACTLY ONE dashed `.rf-edge--rework`.
- `make build-frontend` clean.
- `make lint` clean.
- `git diff main --stat` confirming FE-only (nothing under `backend/`; `TeamNodePanel.tsx` shows no
  change).
- A `READY_TO_MERGE` line.

## Out of scope (do NOT run — save credits)
The live e2e targets (`make team-edit-e2e`, `make steering-e2e`) are visual/agent runs that this
FE-only edge-rendering change does NOT touch (no launch/ship path change) — do NOT re-run them and
do NOT burn NIM credits. The operator re-eyeballs the canvas.

## Branch
Commit on the EXISTING `feat/p1.8b-editable-team` (stack on `48a440e`); never push. The operator
FF-merges after the re-eyeball.
